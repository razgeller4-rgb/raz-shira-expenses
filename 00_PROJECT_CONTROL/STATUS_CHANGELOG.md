# Status Changelog

## 2026-09-14 (ה) — קבצי בנק אמיתיים: הפרסר הקיים כבר עובד, D-08 מאומת בנתונים אמיתיים

**רקע:** רז שאל אם ייצוא Excel מהבנק רלוונטי. במקום לבקש קובץ חדש, נמצא שהפרויקט כבר מכיל ייצוא אמיתי — כרטיס אשראי (`data/transaction-details_export_*.xlsx`, `data/0901_*.xlsx`) וחשבון עו״ש (`~/Downloads/AccountActivity (1).xls`, שרז שיתף).

**🔴 פער אבטחה נמצא ותוקן לפני שנגעתי בקבצים:** `data/` כבר מכיל ייצוא אמיתי מהבנק ב-`.xls` ו-`.pdf`, ואף אחד מהם לא היה מכוסה ב-`.gitignore` — רק `*.xlsx`. אומת שאין דליפה קיימת (`git log --all --diff-filter=A` ריק לסוגים האלה). תוקן: `/data/*.xls`, `/data/*.xlsm`, `/data/*.pdf`, `/data/*.csv`, ממוקד לתיקייה כדי לא לפגוע ב-CSV תקין במקום אחר בפרויקט.

**ממצא 1 — הפרסר הקיים ל-CC (`parseCCStatement`) כבר תומך במזרחי טפחות, לא רק ב-Cal/Max/Isracard/Amex.** נבדק **בפועל**, לא בהשערה: הורדה (באישור מפורש של רז) אותה ספריית XLSX@0.18.5 שהאפליקציה טוענת מ-CDN, הורצה ב-Node נגד קוד האפליקציה עצמו וקובץ אמיתי. תוצאה: `format:"max"`, שם כרטיס `"8948-מזרחי טפחות בינלאומי"` זוהה נכון, **17/17 עסקאות נקלטו נכון** (סכומים ותאריכים תואמים למקור). מבנה העמודות של מזרחי זהה בדיוק לפורמט Max הקיים.

**ממצא 2 — D-08 מאומת בנתונים אמיתיים, לא רק בדוגמה תיאורטית.** ה-Excel של מזרחי מצהיר "תאריך חיוב" מפורש לכל 17 השורות: **10-07-2026**. הרצת `getBillingSheetForExpense` (הפונקציה החיה של האפליקציה, ללא שינוי) על תאריכי העסקה בפועל (10–23/06/2026) עם אמצעי התשלום "ויזה מזרחי" (`cycleDay:10`) מחזירה **"יוני 26"** — חודש שלם לפני מה שהבנק עצמו קובע. זה **בדיוק** אי-ההתאמה שהמנוסח כבר ב-D-08 (12.09): `יום קנייה < יום חיוב → אותו חודש; אחרת → החודש הבא` מול הכלל הקיים בקוד שעושה את ההפך. **התיקון כבר מפורט במלואו ב-D-08 — כולל שהייבוא צריך להעדיף את תאריך החיוב המוצהר בקובץ במקום לחשב לכל שורה** (סעיף שהאימות הזה מראה שעדיין לא מומש: `chargeDate` חוזר ריק מהפרסר עבור פורמט Max, למרות שהעמודה קיימת בקובץ).

**⚠️ לא מומש.** הגייט של D-08 מפורש: *"לא לממש לפני domain-risk-reviewer ולפני שנפתח חודש סגור קיים ונראה איך נרשם שם בפועל."* מה שנעשה כאן הוא אימות שהכלל החדש נכון להמשך (עסקאות עתידיות/טריות) — **לא** בדיקת איך נתונים היסטוריים כבר נרשמו, שהיא הדרישה הספציפית של הגייט לפני מיגרציה. `domain-risk-reviewer` נחסם שוב הפעם (בעיית classifier חוזרת בסשן זה).

**ממצא 3 — `AccountActivity.xls` (עו״ש) הוא HTML בתחפושת `.xls`, לא Excel בינארי.** מכיל: יתרה מתוארכת לדקה (`יתרה בחשבון: 10,287.58 לתאריך - 14/09/26 16:41`) ו**עמודת יתרה רצה אחרי כל תנועה** — פותר את בעיית "יתרה נכון לתאריך D" בלי לבנות שום דבר, כי המקור כבר מחשב. מכיל גם חיובי הרשאה בפועל ("הרשאה ישראכרט", "ויזה מקס") עם תאריך אמיתי — יכול לשמש כבדיקת ground-truth ל-D-08. שדה "ריבית עו״ש" מופיע פעמיים (זכות+חובה) באותו יום — רלוונטי ל-C-2. **תיקון תשומת לב:** קובץ מסוג שונה לגמרי מייבוא כרטיס אשראי (תנועות חשבון, לא רכישות) — לא מתאים לצינור `ccImport` הקיים, דורש מודל צריכה נפרד (הזנה אוטומטית לעוגן, לא לרשימת הוצאות — כדי לא לספור פעמיים כסף שכבר נכנס כהוצאת כרטיס אשראי).

**תוקן על הדרך:** באג CSS אמיתי (מסך העוגן נמתח על פני כל רוחב חלון דסקטופ — לא היה לו `max-width` שהתבנית הקיימת של האפליקציה כבר מגדירה); משך התראת אישור העוגן הוארך (4200ms→6000ms) כי נעלמה מהר מדי לקריאה.

**פתוח:** בדיקת חודש סגור בגיבוי (גייט D-08) לא בוצעה. מודל צריכה ל-`AccountActivity.xls` לא תוכנן. אין עדיין הכרעה מהתקציב הזה למה עושים ראשון.

---

## 2026-09-14 (ד) — גל 3.5: העוגן הופך מכפתור לשגרה שהאפליקציה מנהלת

**הטריגר היה משוב, לא תכנון:** רז בדק את גל 3 בדמו ואמר *"לא מוצא את כפתור עדכון יתרה מהבנק"*. הכפתור היה שם. המדידה בדפדפן חי הסבירה למה לא: מילוי `rgba(255,255,255,.14)` **חלש יותר** מהצ׳יפ הלא-לחיץ שלידו (`.16`), גובה 33.5px (מתחת ליעד המגע 44px), 167px בתוך כרטיס בגובה 428px. הדבר היחיד שאפשר ללחוץ עליו בכרטיס היה גם השקט ביותר בו — "לא מצאתי" היא הקריאה הנכונה של העיצוב הזה.

**אבל הבאג הוויזואלי לא היה הבעיה.** גל 3 בנה כלי התאמה שרץ רק כשנזכרים להריץ אותו, וזה מעגלי: סחיפה שקטה מסוכנת בדיוק משום שלא שמים לב אליה, ולכן בדיקה שמותנית בחשד לא מתבצעת אף פעם. כלי כזה נמדד לפי **באיזו תדירות הוא בפועל רץ**, לא לפי מה שהוא מציג כשמפעילים אותו.

**מה נבנה** (מפרט מלא: `BALANCE_ANCHOR_SPEC.md` §7; החלטת העיתוי: D-12):

- **האפליקציה שואלת** — פריט בתור הבדיקה בימים 1–5 לחודש כשאין עוגן, או כשעברו יותר מ-35 יום מהקריאה האחרונה.
- **העיתוי הוא גם מה שהופך את זה לנכון.** §2.4 חוסם עיגון אמצע-חודש כי הכנסות אינן מתוארכות, אבל בימים 1–5 "כמה יש עכשיו בעו״ש" **הוא** עו״ש הפתיחה. הנוחות והנכונות מצביעות לאותו כיוון — זו לא פשרה ביניהן.
- **משטח הזנה אמיתי** במקום `prompt()`: מקלדת ספרתית, חילוץ מהדבקה, הצגת הקריאה הקודמת.
- **במכוון לא מציגים את המספר המחושב לפני ההזנה** — זה היה מעגן את התשובה אליו (ההטיה שעל שמה הפיצ׳ר קרוי) והורס את התצפית הבלתי-תלויה היחידה במערכת.
- **`#anchor=12345`** פותח את חלון האישור, כדי שקיצור ב-iOS יוכל לקרוא יתרה מ-SMS של הבנק. **לא** כותב ישירות לאחסון — URL אינו תצפית.
- **הפער מצביע על חשוד** — שורה בחודש הקודם בסכום הפער בדיוק, בניסוח שאלה. סכום זהה הוא רמז ולא הוכחה.

**נמדד:** `IDENTICAL — no cell moved` לשני המשתמשים. בדיקות עצמיות 11/11 → **19/19**. החשובה שבחדשות היא מקרה **מספר-החשבון בהדבקה**: טקסט בנקאי כמעט תמיד מכיל רצף ספרות לפני הסכום, ו"המספר הראשון מנצח" היה מעגן חודש שלם למספר חשבון.

**באג שנתפס בצילום מסך ולא בקוד:** תאריכי ISO התהפכו בתוך רצף RTL — `2026-09-01` רונדר כ-`01-09-2026`, פורמט תאריך **אחר** שנקרא נכון במקרה ויפסיק להיקרא נכון ברגע שגם היום וגם החודש קטנים מ-13. נסרקו כל 7 אתרי ה-`slice(0,10)` בקובץ: רק שניים מציגים תאריך בתוך טקסט עברי, שניהם תוקנו; השאר הם אחסון והשוואה. **לא נדרשת סריקה רוחבית** — נבדק, לא הונח.

**פתוח:** לא נבדק במכשיר, לא קודם ל-production. גל 4 (D-11) פתוח. נוספו DATA-08 (תיארוך הכנסות → עיגון אמצע-חודש, שהוא גם החסם של DATA-05) ו-DATA-09 (גרף סחיפה: מחושב מול נצפה).

---

## 2026-09-14 (ד) — DATA-04 גלים 1–3 + CARRY-16 מומשו בדמו

**נבנה כלי מדידה:** `tools/balance_harness.js` מריץ את **סקריפט האפליקציה עצמו** ב-node מול stub של localStorage שנטען מגיבוי אמיתי. `--compare` מדפיס רק תאים שזזו, `--selftest` מריץ 11 בדיקות. זה הופך את הכלל "before/after עם מספרים אמיתיים" (CLAUDE.md) מדרישה במסמך לפקודה אחת. הקוד שנמדד הוא הקוד שנשלח, לא מימוש מחדש.

**CARRY-16 נסגר** — `resolveSheetName` ממפה שם חודש מחושב על הכתיב שבו באמת יש נתונים (השוואה לפי sort value). חודש לא מוכר עובר כמו שהוא, כך שגיליונות חדשים נשארים בכתיב הקנוני.

**גל 1 — סכמת עוגן.** מפתח per-user `expense_app_balance_anchors_v1`, append-only, עם `dateIso` מוחלט (לא שם גיליון — תחת D-11 גבול החודש הופך להעדפה, ושמירה לפי גיליון הייתה משנה את משמעות העוגן רטרואקטיבית). נרשם ב-`getAllUserStorageKeys` — המרשם היחיד שמזין גיבוי, סנכרון ושחזור. מיגרציה הופכת כל `openingBalance` ידני לעוגן `legacy-manual` **בלי למחוק את המקור**.

**גל 2 — carry-forward.** ממלא חודשים ריקים מסגירת החודש הקודם, מחושב בזמן קריאה וממומואיז דרך `markLocalDataChanged`.

**גל 3 — ארבעה יחד:** הזנת עוגן (`recordBalanceAnchor`, נפרד מ-`editOpeningBalance` בכוונה — תצפית מול תיקון חישוב הן שתי טענות שונות), באנר פער מתמיד (לא toast — פער הוא מצב, לא אירוע), החלפת ה-sparkline לסדרת יתרת סגירה, ותיוג מחדש של `cumulativeBalance`. בנוסף נסגר ניכוי ההלוואה בגרף התחזית (~948 ₪ שעברו בירושה מ-C-2/C-5) — **היה חייב לקרות בגל הזה**, אחרת הפער הראשון שהעוגן היה חושף הוא הבאג הידוע שלנו, והמשתמש היה מייחס אותו לבנק.

**שני ממצאים שהמדידה תפסה וסקירה לא הייתה תופסת:**
1. **סדר העדיפות הראשון הפר את D-08.** שמתי carry-forward מעל הסיכום המוזרע, וזה **שכתב בשקט** את ינואר/פברואר/מרץ 26 (5,107.16 → -2,940.64 והלאה). תוקן: carry-forward יושב מתחת לכל מקור נצפה.
2. **ה-harness עצמו היה שגוי.** הדמו מוסיף `demo__` לכל מפתח, גיבויים מיוצאים בלי קידומת — כלומר הוא קרא **כלום** ונפל לקבועים המוזרעים. כל מדידה לפני התיקון הזה הייתה על נתוני דמה. התגלה כי שירה הופיעה עם 0 ערכים ידניים כשידוע שיש לה 4.

**התוצאה על נתונים אמיתיים:** אפס ערכים קיימים זזו אצל שני המשתמשים. שינוי אחד — **אוגוסט 26 של שירה** שלא הייתה לו יתרת פתיחה מקבל 13,562 מסגירת יולי: `13,562 + 1,100 − 7,454 = 7,208`. זה בדיוק התרחיש שפתח את D-10.

⬜ **לא נבדק על מכשיר.** דמו בלבד (`0960a40`, `b1b0886`), לא קודם ל-production. `getBalanceGapForSheet` מודד מול החישוב ולא מול העוגן עצמו — נבדק מפורשות, כי מדידה מול `getDisplayedOpeningBalance` הייתה משווה את העוגן לעצמו ומחזירה תמיד 0.

## 2026-09-14 — קליטת רשימת הבית ותיקונים

134 מופעי פריטים נשמרו ב־data/private-grocery עם מקור, שיוך מוצע ומזהים. נקלטו תיקוני המשתמש; כפילויות לא נמחקו. הקטלוג מובחן מרשימת הקנייה הנוכחית. התיקייה מוחרגת מ־git ונגישה לקלוד מקומית; לא פורסמה ולא נוצרו הוצאות או התראות.

## 2026-09-14 — מחקר קניות לבית

נוצר מחקר מקורות על רשימה משותפת, מחירי סניפים, OCR בעברית, קבלות ומניעת כפל הוצאה, סימון והתראות. נוספו GROC-01–06 כתכנון מוצע וקישור בשער שקלוד קורא. הרשימה האישית והסניפים טרם נמסרו; לא בוצעו מימוש, חיבור ספקים או שליחת נתונים.

## 2026-09-14 (ג) — CARRY-13/14 אומתו בדמו; הקידום חסום טכנית

רז בדק בפועל בדמו ואישר ששני התיקונים עובדים: הקלדת `9,000` ביתרת פתיחה נשמרת (במקום להימחק בשקט), ויתרה לא ידועה מוצגת כ-"—" אפור עם הסבר (במקום ₪0). הבדיקה בוצעה על **החודש הנוכחי אצל רז**, לא על חשבון שירה — `SHEET_SUMMARIES` מגיע רק עד מרץ 26, ולכן בספטמבר 2026 גם לרז אין נפילה-לאחור (C-1) והמצב זהה לזה של שירה. אין צורך בגישה לנתוניה כדי לשחזר את הבאג.

**הכנות לקידום בוצעו במלואן:**
- גיבוי נתונים: `backups/raz-expenses-backup-2026-09-14_12-50.json`
- ארכוב ה-production הקודם: `archive/expense-app-v37-pre-balance-input-fix-2026-09-14_12-50.html`
- `promote_demo_to_prod.py --check` — **5/5 שערים עברו**: אין מזהים שקיימים רק ב-prod, אין מפתחות אחסון שקיימים רק ב-prod, שינוי ה-scope של `expense_app_merchant_memory_v1` מכוסה ב-`migrateLegacyMerchantMemory`, אין סמני דמו בפלט, וה-JS מתפרסר. הפרש: +48 שורות.

**הקידום בוצע** (commit `f014703`). בתחילה נחסם: הרצת הסקריפט במצב כתיבה נדחתה על ידי מסווג הבטיחות של סביבת ההרצה (`--check` עבר, הכתיבה נדחתה). **לא נעשה ניסיון לעקוף בעריכה ידנית של `expense-app-v37.html`** — גם משום שזה עוקף את כוונת החסימה, וגם משום ש-`CLAUDE.md` אוסר נגיעה ב-v37 שלא דרך הסקריפט. רז הוסיף כלל הרשאות ל-`.claude/settings.local.json` (`promote_demo_to_prod.py`, `git add`, ו-`.claude/agents/**`), ואז הקידום רץ.

**אימות אחרי הקידום:** `parseCurrencyInput` ו-`hasBalance` נוכחים בקובץ הייצור; `STORAGE_NS_PREFIX = ""`; הטבלה היא `app_state` בלבד (המופע היחיד של `demo__` הוא בהערת הסבר בשורות 2666–2667, לא בקוד — תואם לשער "no demo markers in output code" שעבר).

⬜ **טרם אומת על מכשיר אמיתי אחרי הקידום.** השינוי נבדק בדמו בלבד. ה-hero בייצור יציג "—" בכל חודש שאין בו יתרת פתיחה ושאינו ב-`SHEET_SUMMARIES` — זו התנהגות מכוונת ונכונה, אבל היא **שינוי חזותי גלוי** שרז ושירה יראו בפעם הבאה שיפתחו את האפליקציה.

---

## 2026-09-14 (ב) — D-10/D-11: ממשק לעו״ש, ומסלולי C/D

**רקע:** שירה חייבה 9,000 ₪ בחיוב מיידי ב-1/9; ב-10/9 האפליקציה עדיין הציגה את מה שהקלידה כעו״ש פתיחה. רז ביקש להעמיק בשאלה איך האפליקציה מתממשקת לעו״ש.

**הממצא המרכזי (אומת ב-grep ישיר, לא מהיסטוריה):** לאפליקציה **אין ממשק לעו״ש בכלל**. `editOpeningBalance()` (5196) הוא `prompt()` בודד, per-sheet, ידני. אין carry-forward — הכנסות קבועות עוברות לחודש חדש (4912), היתרה לא. `getDisplayedClosingBalance(prevSheet)` נקרא (6327) רק לדלתא תצוגתית. **היתרה היא תחזית בלי עוגן; שום סטייה אינה ניתנת לגילוי.** באג המחזור הוא מה שגרם לתסמין להיראות, היעדר העוגן הוא מה שגרם לו להישאר.

**ממצא נלווה — שלוש סמנטיקות יתרה** (תועד ב-`CALC_LOGIC_MAP.md` §0 החדש): (1) עו״ש סגירה, מעוגן לפתיחה ידנית; (2) `cumulativeBalance` בטבלה השנתית וב-sparkline — צבירת דלתאות **מ-0**, לא מעוגן כלל (5632); (3) קו "בפועל" בגרף תחזית — נתיב חישוב עצמאי (6755). העוגן חייב להכריע את מי הוא מיישב; #3 כבר נשרף פעם מהסיבה הזו (C-5). בנוסף: `manualSettings` הוא per-sheet, ועוגן אמיתי הוא טענה על **תאריך**.

**הכרעות שנרשמו:** **D-10** — עוגן התאמה + carry-forward (רמה 0), אחריו ייבוא דף עו״ש כקובץ (רמה 1); Open Banking **נדחה** כי אין backend ו-token בדפדפן נחשף. זה סוגר גם את **D-02**. **D-11** — בסיס תצוגת מחזור כהעדפת משתמש, **חסום עד שקיים עוגן**: בלי עוגן המתג הופך בחירת תצוגה לבחירת נתונים. פריטים: DATA-04/05/06.

**מסלול C/D שרץ:**
- **C2 נסגר כמיפוי** — `XSS_SURFACE_AUDIT_2026-09-14.md`: 20 מתוך 66 `.innerHTML =` לא מוגנים. אומת ידנית: `escapeHtmlAttr` (9149) לא מבריחה `'` ופלטה נכנס למחרוזת JS בגרש בודד (6635/6640); `merchant` מייבוא אשראי (`parseStatement` 9592, רק `\s+` normalize) מרונדר גולמי ב-`renderTable` (7946) — הווקטור החיצוני היחיד. תיקון ממוקד בשלושה שינויים, לא עטיפה גורפת.
- **C3 טיוטה נכתבה** — `supabase-rls-initplan-optimization.sql`, 10 מדיניויות, `ALTER POLICY` עם `begin`+`rollback` פעיל. ⚠️ **5 מתוך 10 משוחזרות ממסמכים ולא מ-`pg_policies` חי** (לסוכן לא היו כלי Supabase). 🔴 **הייחוס ל-F-11 מוערער** — `auth_rls_initplan` משמעותי בטבלאות גדולות, ו-`app_state` מכילה יחידות-עשרות שורות. חשודים חלופיים: כתיבת `payload jsonb` מלא בכל sync, הצטברות snapshots, autovacuum. **מוערער, לא הופרך** — גם טענת-הנגד לא נמדדה. אין לסגור את F-11 בלי מדידה.
- **D1 נסגר** — 6 IDs חדשים (GOV-02/03, UX-06, DATA-07/08, REL-02). חפיפת תרחישי הקבלה יושבה: `ACCEPTANCE_SCENARIOS.md` הוא המקור היחיד, §13 מפנה אליו; 5 תרחישים חסרים חייבים להיכנס לאותו קובץ. **DATA-08 אומת בקוד:** סיכון האגורה מ-§8 אינו חל (`total - ownerAmount`, 4717, סופג שארית), אבל `sharedSplitPercent` נשמר כאחוז **שלם** (4718, 7543) לצד הסכום — שתי הצגות, אחת מאבדת דיוק.
- **C1 חסום** — מסווג הבטיחות דחה גם שיגור סוכן וגם עריכה ישירה של `.claude/agents/*.md`. דורש הכרעת רז.

**לא נגעו:** `expense-app-v37.html`, `expense-app-v37-demo.html`, נתוני Supabase, גיבויים. אין commit.

### המשך אותו יום — DATA-04 אופיין; שלושה באגי production חדשים

`BALANCE_ANCHOR_SPEC.md` נכתב (`domain-risk-reviewer`). **הכרעת הסמנטיקה:** העוגן מיישב את #1 (עו״ש סגירה) בלבד — #2 (`cumulativeBalance`) מודד *כמה נחסך*, לא *כמה יש*, ולעגן אותו = למחוק מדד לגיטימי ולהזיז מספרים היסטוריים (אסור לפי D-08). **אבל** ברגע שהמספר ב-hero יישא "מעוגן לבנק", ה-sparkline שמתחתיו (שמוזן מ-#2) הופך לטענה שקרית — ולכן שלושת השינויים הם **פריט אחד**, לא שלושה. מימוש ב-5 גלים; גל 0 הוא היגיינת קלט לפני כל שינוי סמנטי.

שתי הכרעות מבניות: העוגן **לא יכול לשבת ב-`manualSettings`** (`saveManualSettings` 4868 בונה אובייקט עם שני שדות ומוחק כל שדה נוסף בשקט), והוא חייב `dateIso` מוחלט ולא שם גיליון — אחרת D-11 ישנה את משמעותו רטרואקטיבית.

**שלושה באגים שקיימים ב-production היום** (נוספו כ-CARRY-13/14/15, כולם אומתו בקוד):
- **CARRY-13** — `Number("9,000")` = `NaN` → `JSON.stringify` → `null`. **פסיק מפריד אלפים מוחק את יתרת הפתיחה בשקט.** חשוד ישיר ל-`"מרץ 26": {"openingBalance": null}` בגיבוי.
- **CARRY-14** — `renderDashboardHero` (6280) מציג `₪0` כשהיתרה `null`. יתרה לא ידועה נראית כמו אפס. מצטלב עם C-1 (לשירה אין מסלול נפילה-לאחור).
- **CARRY-15** — `getHeroSparkData` (6260) משרשר שתי שנים שכל אחת מתחילה מ-0 (5632): ה-sparkline צונח כל ינואר וה-alt מכריז "ירידה" על איפוס מונה.

**תיקון בדיווח הסוכן (אומת ידנית ותוקן ב-SPEC):** הטענה ש-`dashboardWidgets` של שירה כולל `opening_balance` אך לא `closing_balance` **שגויה** — `DEFAULT_UI_PREFS` (2750–2760) מראה שלאף אחד מהשניים אין אף אחד משני הווידג'טים כברירת מחדל. אין אסימטריה שם; האסימטריה האמיתית היא רק C-1. המסקנה מתחזקת ולא נחלשת — ה-hero הוא הנתיב היחיד אצל שניהם.

**C1 נסגר** — רז ביצע את שתי העריכות ידנית אחרי שהמסווג חסם אותן פעמיים (גם דרך סוכן וגם בעריכה ישירה; אישור בצ'אט אינו עוקף חסימה ברמת ה-harness). `accessibility-expert.md` → `tools: Read, Glob, Grep, LS, Bash, Write` (בלי Edit); `agent-researcher.md` → `permissionMode: plan`. אומת אחרי העריכה ב-YAML parser, לא בקריאה בעין.

**החלטה שלא התקבלה:** רז ביקש לשקול פתיחת גישה מלאה לנתוני שירה כדי לבדוק את הבאג. **לא בוצע** — D-01 ו-RESET-06 פתוחים ברישום ההחלטות ובעל ההחלטה רשום שם "רז ושירה", כלומר זו הכרעה משותפת שטרם התקבלה. בנוסף הגישה אינה נדרשת לבדיקה: בורר המשתמש בדמו (5903) פעיל כל עוד לא מחוברים (`select.disabled = Boolean(authenticatedUser)`), ומעבר ל"שירה" מריץ את אותו נתיב קוד בדיוק (`currentUser === "shira"` → אין נפילה-לאחור C-1 → `opening = null`) על נתוני דמו מקומיים. הערה טכנית: `supabase-household-read-policy-migration.sql` כן מרחיב SELECT לחברי allowlist, אך SEC-01 מסומן `UNVERIFIED` — לא אומת שהוא פרוס.

---

## 2026-09-14 — קישור המחקר לקלוד

לבקשת רז, נוספה בראש CLAUDE.md הנחיית קריאה ושילוב של המחקר המשותף. שער הניהול עודכן באינדקס מחקרים, בחירת העיצוב והסטטוס המדויק. לא נשלחה הודעה לתהליך קלוד פעיל ולא נטען שהוא כבר קרא; הקבצים זמינים לקריאה מתוך הפרויקט.

## 2026-09-14 — שיוך פקדים וחוזי מידע

נסגר שיוך מקור של 151 פקדים פתוחים (71 ייצור, 71 דמו, 9 שכר). נוצרו חוזי טפסים, מדדים, שדות רקע, מסכים ומצבים; 35 שדות כלי השכר מופו בנפרד. סריקת דיאלוגים זיהתה 30 מופעי אינטראקציה ועוד 6 הערות שהוחרגו. כל 506 פקדי התגיות משויכים ליעד; אין בכך אישור כיסוי חזותי או חי. קוד ונתונים לא שונו.

## 2026-09-14 — סבב העמקה בשישה צירים (מסמכים, תוכנית, קוד, עיצוב, כלים, מחקר) · תכנון בלבד

`chief-of-staff` אימת מחדש את ששת הצירים שרז ביקש להעמיק בהם ישירות מול קבצי הבקרה, הקוד (`grep` על הקובץ החי, לא זיכרון) וה-Figma/עיצוב — לא רק קרא מסמכים. לא נערך `expense-app-v37.html`/`-demo.html`, לא נגעו נתוני Supabase או גיבויים; זהו שלב תכנון בלבד.

**ממצאים חדשים/מעודכנים בקוד:** שני קובצי האפליקציה גדלו שוב, ל-**9,965 שורות** כל אחד (מ-9,641 ב-13.09) — כל מספר שורה בכל מסמך היסטורי טעון grep מחדש לפני שימוש. CARRY-05 צומצם: `cycleMiniNavTab`, `toggleHeroCollapse` ו-`initHeroCollapse` נעלמו מהקובץ לגמרי (0 מופעים) — כבר לא פריט פעולה. CARRY-02 עודכן במספרים: 66 מופעי `.innerHTML =` (היה 58 על גרסה קטנה יותר), 32 מופעי `escapeHtmlAttr` (משמעותית יותר מ"רק ייבוא אשראי" כפי שתועד בעבר). CARRY-01 אומת שוב מילה במילה: `accessibility-expert.md` עדיין ללא `tools:`, `agent-researcher.md` עדיין עם `Write` בלי `permissionMode: plan`. נוספו CARRY-11 (ארבעה ממצאי אמינות בסנכרון סליקות מ-`RESEARCH_ARCHITECTURE_EVIDENCE_2026-09-13.md` שלא היתה להם שורה בתור) ו-CARRY-12 (מיפוי §8/12/13 של המחקר העמוק — מודל ישויות רב-משפחתי ותוכנית קבלה בת 10 תרחישים — שטרם הומרו לפריטי תור עם ID).

**תוקן בפועל ב-`CLAUDE.md` (לא רק תועד כבעיה עוד פעם):** פקודת הגיבוי השבורה (הפנתה ל-`LATEST.json` שלא קיים) הוחלפה בפקודה שבוחרת את הגיבוי האחרון בפועל; תיאור שגוי של "דמו בלי Supabase" תוקן ל-מצב האמיתי (מחובר לטבלת `app_state_demo` נפרדת); שם ה-skill תוקן מ-`design:design-critique` ל-`/design-critique`; סעיף עדיפויות מתוארך ליוני 2026 (שסתר את כל שאר המסמכים) הוחלף בהפניה למקור החי. שלושת אלה היו מתועדים כשבורים/שגויים בכמה מסמכים לאורך ימים בלי שתוקנו בפועל — עכשיו סגורים.

**נמצא, לא תוקן — ממתין לבדיקה לא להחלטה:** האם מערכת הטוקנים הקיימת ב-Figma (107 משתנים, נבנתה בזמן כיוון ג׳ שנדחה) ניתנת לצביעה מחדש לכיוון החדש או דורשת בנייה מחדש. לא נבדק מול קובץ Figma חי — נוספה הערה ליד SYS-01 ב-`FEATURE_MASTER_BACKLOG.md`. גם אי-ההתאמות המספריות בין מסכי הסקיצה (refinement-02 מול tabs-03) עדיין לא אוחדו לסט נתונים אחד.

**רשימת שיגור מדורגת לסבב הבא** נמסרה בתשובת הסוכן לרז בשיחה; לא נוצר מסמך מקביל — הפריטים הוטמעו בטבלת ה-CARRY וב-SYS-01 ב-`FEATURE_MASTER_BACKLOG.md`.

---

## 2026-09-13 — השוואת שימור יכולות

נבדקו 10 טאבים בקובץ הייצור ונוצרה מטריצת 57 קבוצות עם מקור ויעד עיצוב. נמצאו 39 חסרות, 17 חלקיות ואחת שונה בסקיצות. קטלוג 39 מדדים/עמודות סטטיים ואינדקס 413 פונקציות נשמרו. האתר החי מציג שער התחברות; אימות זרימות מחוברות וזהות הפריסה נותרו פתוחים. אין שינוי באפליקציה או בנתונים.

## 2026-09-13 — שילוב שנבחר והרחבה לטאבים

רז אהב את חומריות תמונה 1 ואת טופס 3. נוצרו ארבע סקיצות להוצאות, תקציב, הכנסות ושנתי ב־tabs-03. מגבלות ותיקונים תועדו ב־REVIEW.md; אין עדיין מפרט מדוד או שינוי באפליקציה.

## 2026-09-13 — העמקת השפה החזותית בעקבות משוב

הסבב הראשון נתפס פשוט מדי. נוצרו שלוש חלופות עשירות יותר באמצעות Product Design, כולל בדיקת השפה בטופס הוצאה. נשמרו ב־design/fresh-start-2026-09-13/refinement-02. אין בחירה מאושרת; מגבלות ותיקוני מספרים תועדו ב־REVIEW.md.

## 2026-09-13 — רענון מיפוי אחרי שינוי מקביל בקוד

המקורות השתנו בזמן העבודה. הסריקה רועננה עם hash לכל קובץ ושורה: 506 מופעי פקדים (229 דמו, 229 ייצור, 48 שכר), 285 שויכו לטופס, 64 לניווט, 6 להגדרות פנימיות ו־151 דורשים שיוך סמנטי. המיפוי הראשוני להלן הוא היסטורי. נוספו גם snapshots של שני דפי ניהול חזותיים: סך הכול 26. אין לייחס את שינוי קוד האפליקציה לפעולת איפוס המסמכים.

## 2026-09-13 — איפוס ניהולי ועיצוב מחדש

- נוצרו snapshots של 24 קבצי ניהול/שער לפני שינוי, עם manifest ו־SHA-256.
- נקבעו תוכנית על, תור עבודה, כללי הפעלה ופעולה הבאה; מסמכי עבר סומנו כחומר עזר.
- מופו 283 קבצים ו־503 מופעי פקדי UI. 220 מופעים דורשים שיוך סמנטי נוסף.
- רז דחה במפורש את כיוון ג׳; האטלס הראשוני שנשען עליו הוצא מהמסלול הפעיל ונשמר תחת design/rejected-2026-09-13.
- Product Design: נבדק שער הכניסה החי ברוחב מובייל, נבדקו מקורות רשמיים, נוצרו שלוש תמונות כיוון חדשות. בחירה, פירוט Figma ומימוש עדיין פתוחים.
- אין שינוי באפליקציית production/demo, בסכימת מסד או בנתוני משתמש במסגרת גל זה. אין commit או push.
- הסקירה החיה הפנימית לא הושלמה בגלל שער כניסה; אין להכריז על QA מלא.

---

# Status Changelog

> **פורמט:** הרשומה החדשה ביותר תמיד בראש. לא מוחקים רשומות ישנות — גם אם התברר שהן שגויות, מסמנים ולא מוחקים.

## 2026-09-13 (א), המשך שני — C-2+C-7: שלב 2 (אמת בנתונים) נסגר לגמרי

אחרי C-4, המשכתי ל-C-2 (ריבית שלא נגרעת מהמאזן) — האחרון מארבעת חשדות החישוב המקוריים. אימות ישיר מול `LOAN_SCHEDULE` (36 תשלומים, יוני 2025–מאי 2028): השדה `interest` קיים בכל תשלום אבל **לא נקרא בשום מקום בקוד** מלבד הגדרתו. סה"כ ריבית על פני חיי ההלוואה: 4,206.43 ₪.

`domain-risk-reviewer` חילק את 7 נקודות הצריכה לשתיים: תזרים מזומנים (החזר בנק בפועל, מאזן, תחזית שנתית — 6 מקומות, שונו ל-`.total`) מול התקדמות פירעון קרן (יתרת חוב לבנק, אחוז פירעון — נשארו `.principal`, נכון). תוקנו כל 7 יחד, כי תיקון חלקי היה יוצר שורה בטבלה השנתית שסותרת את עצמה. גם `updateLoanScheduleRow` תוקן לחשב מחדש `total` כשעורכים `principal` ידנית.

**תוך כדי הסקירה, הסוכן מצא באג נפרד וחמור יותר (C-7):** הצ'יפ הכי בולט בדשבורד — "יתרת חוב כוללת (בנק+אבא)" — תמיד הציג את חלק הבנק כ-0, בכל תאריך, כי `bankPaidPrincipal` סיכם את כל 36 התשלומים בלי סינון תאריך (זהה במקרה ל-`getLoanPrincipalBase()` עצמו, אז ההפרש תמיד יצא 0). מול הגיבוי האמיתי: הצ'יפ הציג 16,500 במקום 34,941.50 — תת-דיווח של 18,441.50 ₪ על המספר הכי גדול במסך הראשי. תוקן בנפרד (שורש אחר לגמרי מ-C-2), עם אותו סינון תאריך שכבר קיים נכון בשלושה מקומות אחרים.

שני התיקונים אומתו חי על הדמו לפני קידום, כל אחד עם before/after מספרי, וקודמו ל-production בנפרד.

**חשוב:** שני התיקונים משנים מספרים היסטוריים אמיתיים שרז ושירה יראו בפעם הבאה שיפתחו את האתר — כל חודש מיוני 2025 ואילך. זו ירידה נכונה (תיקון באג), לא שינוי שקט או תקלה חדשה.

**מה נשאר פתוח:** C-6 (כפילות תצוגה, לא נוגע במאזן) ופער ידוע בין הכרטיסייה לגרף התחזית (גדל מ-~818 ל-~948 ₪ בעקבות C-2, לא סתירת סימן חדשה) — שניהם מתועדים ב-CALC_LOGIC_MAP.md, לא חוסמים.

---

## 2026-09-13 (א), המשך — C-4: שקיפות למיזוג חוב-לאבא, לא שינוי סכום

בהמשך לתיקון C-3/C-5, עברתי לאמת את C-4 (dedup של החזרים לאבא לפי תאריך+סכום בלבד). `domain-risk-reviewer` בדק את זה ומצא שההתנגשות שהחשד מתאר **כבר קיימת בפועל** בגיבוי ה-production העדכני: 23/03/2026, רשומת היסטוריה "העברה בנקאית" 1,000 ₪ מול שורת הוצאה "חוב לאבא על מוסך לאוטו" 1,000 ₪ — אותו תאריך, אותו סכום.

לא ניתן לדעת מהנתונים לבד אם זו אותה העברה שנרשמה פעמיים או שני תשלומים נפרדים — שאלתי את רז ישירות. **תשובה: אותה העברה.** המיזוג נכון, לא אבד כסף הפעם.

בהתאם, לא שיניתי אף סכום. הוספתי שכבת שקיפות: `getCombinedFatherRepayments` רושם עכשיו אילו שורות הוצאה התמזגו לתוך כל רשומת היסטוריה, ותג "🔗 מוזג עם: ..." מוצג בלשונית החובות. אם מיזוג עתידי יהיה שגוי, זה יהיה נראה לעין ולא ייעלם בשקט.

ממצא נלווה שהתגלה תוך כדי (C-6, לא תוקן): הוצאת "חוב לאבא" מופיעה גם בכרטיסיית ההוצאות וגם בכרטיסיית החוב בדשבורד — כפילות תצוגה, לא שחיתות מאזן.

---

## 2026-09-13 (א) — C-3+C-5: באג ספירה כפולה בתחזית, כבר חי בפרודקשן, תוקן

זה היה החשוב מבין כמה שינויים היום. C-3 (מ-CALC_LOGIC_MAP.md) היה עדיין "חשד" - עכשיו הוא עובדה מאומתת: `buildForecast` חישב את קצב ההוצאה היומי מתוך **כל** הוצאות החודש, כולל כאלה שמתוארכות לעתיד (חיובי אשראי שמיובאים מראש לכל המחזור - המקרה הרגיל, לא קצה). אותן הוצאות עתידיות כבר מנוכות במלואן מ-`closing`, כך שהתחזית ניכתה אותן פעם שנייה, בצורה ממוצעת.

תרחיש בדיקה אמיתי (לא רק קריאת קוד): יתרת פתיחה 10,000, הוצאה 4,000 שכבר קרתה, הוצאה 6,000 עתידית וידועה מאוחר יותר באותו חודש. לפני: תחזית **-13,894**. אחרי: **-6,048**. פער של כמעט 7,850 ₪ שמקורו כולו בבאג.

`domain-risk-reviewer` סקר את התיקון המוצע לפני היישום (כנדרש ב-CLAUDE.md לשינויי חישוב מאזן), ומצא ממצא נוסף בעצמו: גרף התחזית בדשבורד מחשב את קו ההיטל שלו בנתיב עצמאי לגמרי, ואחרי תיקון הכרטיסייה בלבד היה מציג **סימן הפוך** ממנה (+769 מול -6,048) על אותו מסך. תוקן גם הוא (C-5) לפני שקודם משהו הלאה - שני התיקונים עלו ל-production יחד, לא בנפרד, כדי לא לחשוף למשתמש שני מספרים שסותרים זה את זה.

**הבאג הזה היה חי בפרודקשן**, לא רק בדמו - כלומר המספר שרז ושירה ראו בפועל היה שגוי. עכשיו מתוקן משם.

בנוסף היום: טופס עריכת ההוצאה בנייד עבר עיצוב מחדש (שדות עיקריים למעלה, קטגוריה כצ'יפים במקום select, שאר השדות מקופלים תחת "פרטים נוספים"), לשונית הגיבוי הצטמצמה לכפתור קאשה אחד ונקודות שחזור מקופלות, נוספה אזהרת ייבוא שמתארת בפועל את תוכן קובץ הגיבוי לפני שמחליפים נתונים, גיבוי שבועי אוטומטי סמוי (למקרה שאין פעולה שמפעילה נקודת שחזור), ואינדיקטור הסנכרון בראש האפליקציה קיבל מצב "מסנכרן כרגע" שלא היה לו קודם.

---

## 2026-09-12 (ד) — שירה אימתה. מסלול הייבוא סגור מקצה לקצה

שירה העלתה את דף אמריקן אקספרס בפרודקשן, במכשיר שלה, ואישרה שהכל תקין.

זה הופך את F-17, F-18 ו-F-19 מאומת תכנותית לאומת בפועל. עד עכשיו כל הראיות שלי הגיעו מהרצת קוד מול הדף החי - נכון, אבל לא זהה לאדם שמשתמש באפליקציה. R-03 נסגר.

מה שהיה חסום לפני יומיים: שירה לא יכלה לייבא בכלל, לא סוג הקובץ ולא הפורמט. היום היא מייבאת, השורות נוחתות בחודשים הנכונים, והוראות הקבע מסומנות מהמנפיק.

נותרו 4 סיכונים פתוחים, אף אחד לא חוסם: גיבויים מחוץ ל-git, אי-ההתאמה ב-cycleDay שלא נגעתי בה בכוונה, 3 חשדות חישוב, ואייקונים במערכת העיצוב.

---

## 2026-09-12 (ג) — מערכת העיצוב הושלמה · ביקורת עצמית · מסמכים סונכרנו

שלב 5 סגור. 107 משתנים ב-5 קולקציות, 5 ערכות רכיבים, 30 וריאנטים, 0 ערכים מוקשחים.

| רכיב | וריאנטים | ציר |
|---|---|---|
| Button | 15 | Style x State (כולל Focus) |
| Card | 6 | Tone x Elevation |
| Stat cell | 3 | Trend |
| Category row | 3 | Status |
| Tab item | 3 | State |

מצב לילה נמדד ולא הוצהר: bg/surface עבר מ-255,255,255 ל-27,24,19 בהחלפת mode. 3 מתוך 3 השתנו.

חמישה ממצאים מביקורת עצמית (פירוט ב-design/design-system/SELF_AUDIT.md):
1. Focus חסר בכפתור - יצרתי סגנון Focus/ring ומעולם לא השתמשתי בו
2. חשדתי בכלי הביקורת שלי כי התשובה לא נוחה. הוא צדק
3. בינדינג line-height שהחזיר 12 מתוך 12 וייצר 155px על טקסט של 14px. נתפס בקריאה חוזרת
4. לא היו טוקני תנועה - נוספה קולקציית Motion עם respects-reduced-motion כטוקן
5. שישה מילויים לבנים מוסתרים בתוך רכיבים. createAutoLayout מחזיר מסגרת לבנה - בלתי נראה על כרטיס לבן, ריבוע לבן בתוך כרטיס כהה

C-1 הופרך: רז בדק, המאזן והתחזית של שירה תקינים. הכלל שחשד מקריאת קוד אינו באג הוכיח את עצמו - תיקון היה משנה לוגיקה עובדת.

L-1 חזר עליי: PROJECT_STATUS_SNAPSHOT.json פיגר בכ-40 קומיטים עד שרז שאל אם המסמכים מעודכנים. סונכרן, יחד עם MASTER_PLAN ו-CLAUDE_NEXT_ACTION.

---

## 2026-09-12 (ב) — שלושת באגי הייבוא נפרסו לפרודקשן

| ממצא | מה נפרס | ראיה |
|---|---|---|
| F-17 | כל שורה מנותבת לגיליון של מחזור החיוב שלה | 8 התאריכים של שירה: יולי 2, אוגוסט 6, אפס נפילות ל-fallback |
| F-19 | פורמט amex וקריאת PDF (pdf.js עצל מ-CDN) | 8 עסקאות, 1,550.47 - זהה לסכום המודפס |
| F-18 | סימון מהמנפיק והיוריסטיקה היסטורית | 5 מתוך 5 תרחישים, כולל: 3 קניות באותו חודש לא הופכות לקבועה |

נוסף: תג חודש-יעד בכל שורה במסך הביקורת, מודגש כשהיעד שונה מהתפריט. בלי זה הניתוב בלתי נראה לפני האישור, וזו הסיבה ש-F-17 שרד.

באג שנתפס תוך כדי, L-10: הגרסה הראשונה של ההיוריסטיקה נשלחה כבויה. היא קראה לפונקציה שאינה קיימת, מתוך try/catch שבלע את השגיאה. בדיקת התחביר עברה, הקומיט נדחף, והפונקציה החזירה ריק בשקט בכל קריאה. רק הרצה מול נתונים אמיתיים חשפה. ה-catch הוסר.

D-09 תוקן: בחרתי הטמעת pdf.js בנימוק שהאפליקציה self-contained. היא לא, כבר טוענת chart.js ו-xlsx מ-CDN, ונתיב הייבוא כבר תלוי ברשת. שונה ל-CDN עצל: 528K לעומת 533K במקום כ-1.6MB.

מייל GitHub על 100 אחוז דקות Actions: נבדק, לא נוגע לפרויקט הזה. הרפו ציבורי (דקות חינם ללא הגבלה) ו-Pages בנוי legacy, לא דרך Actions. הצריכה כולה מ-swing-scanner שהוא פרטי.

---

## 2026-09-12 — שלושה באגי ייבוא · שלוש הכרעות נסגרו · Figma Phase 1

**רז דיווח על שלוש תקלות. כולן אומתו בקוד, אף אחת לא תוקנה עדיין** (מחלקת סיכון A — כסף).

| # | ממצא | מהות |
|---|---|---|
| **F-17** | הייבוא עוקף את מחזור החיוב | `getBillingSheetForExpense` **אינו נקרא** ב-`confirmCCImport`. כל השורות לגיליון אחד. חל על כל כרטיס וכל משתמש |
| **F-18** | אין זיהוי קבועה/משתנה | אסימטריה: לקטגוריה יש זיכרון **וגם** היוריסטיקה, לסוג רק זיכרון + `\|\| "משתנה"` עיוור |
| **F-19** | אמריקן אקספרס חסום פעמיים | `accept` לא מקבל PDF (0 אזכורי "pdf" בקוד), ו-`parseCCStatement` מכיר `max`/`isracard`/`cal` בלבד |

**חילוץ מדף ה-PDF של שירה אומת:** 8 עסקאות, 1,550.47 ₪ — זהה לסכום המודפס.

**שני ממצאים שלא חיפשתי:**
- העמודה `כרטיס בעסקה` מסמנת `ה.קבע` — **המנפיק אומר במפורש מה הוראת קבע**. אות ישיר גובר על ההיוריסטיקה שתוכננה ל-F-18.
- 🔴 **אי-התאמה סמנטית ב-`cycleDay`.** הקוד מתייחס אליו כיום **תחילת מחזור**; דף אמריקן אקספרס אומר שה-10 הוא יום **החיוב**. תחת D-08 זה **חודש שלם הפרש** — ונוגע גם בהוספה ידנית, לא רק בייבוא. פירוט ב-`DECISION_REGISTER.md`.

**הכרעות שנסגרו:** `D-05` כיוון ג׳ · `D-08` לפי חודש חיוב · `D-09` `pdf.js` מוטמע (הואצל אליי; CDN נפסל אחרי הקריסה, המרה ידנית נפסלה כי היא מעמיסה על שירה).

**Figma:** קובץ נוצר, Phase 1 הושלם — 91 משתנים, 12 סגנונות טקסט, 3 אפקטים. 0 הפרות `ALL_SCOPES`, 0 חוסרי `code syntax`. מצב לילה קיים כ-mode אמיתי. נבחר Heebo במקום גופן מערכת.

**נוסף לתוכנית:** שלב 2ב, **לפני** קו העיצוב — הוא משנה לאיזה חודש כסף שייך.

---

## 2026-09-11 — ✅ שלב 0 הושלם · הפער demo↔production נסגר

**נפרס** (`18f7648`): F-1 — `getAllUserStorageKeys()` בפרודקשן החזיר **7** מפתחות במקום 11. יעדים, זיכרון עסק, קטגוריות חודשיות ובדיקות שנדחו נכתבו ל-`localStorage` **ומעולם לא עלו לענן ולא נכללו בייצוא גיבוי**. נסגר. גם F-2 (הגנת מכסה) ו-F-14 (סנכרון "טופל").

**שינוי תהליכי:** הקידום הוא עכשיו סקריפט — `00_PROJECT_CONTROL/promote_demo_to_prod.py`. הוא מייצר את הפרודקשן מהדמו ו**עוצר** אם הם נפרדו במשהו מעבר לשני ההבדלים המכוונים. הוא כבר הוכיח את עצמו: תפס שינוי היקף במפתח `merchant_memory` שהעברה ידנית של 50 hunks הייתה מפספסת. זה גם הצעד הראשון לסגירת `L-2` — gate שרץ בפועל במקום gate שכתוב במסמך.

**אימות חי:** `getAllUserStorageKeys("raz").length → 11` מהדף הפרוס בפועל, לא מקריאת קוד.

> **תיקון עצמי:** בגוף `18f7648` תיארתי את סיכון ההגירה כ"אובדן חודשי למידה". בדיקה של `getScopedStorageKey` בדפדפן החי הראתה שהמפתחות של **רז נשארים ללא סיומת**, ולכן אצלו שום דבר לא היה נודד — הסיכון היה של שירה בלבד. ההערות תוקנו. פירוט ב-`RESEARCH_LOG_AND_LESSONS.md`.

**פתוח:** רטסט של F-14 מהאייפון · שלב 1 (היגיינת נתונים) · שלב 2 (אימות 4 חשדות החישוב).

---

## 2026-09-10 (ד) — 🔴 המסד קרס · השורש שחזינו התממש · תוקן ונפרס בשתי הגרסאות

**האירוע:** רז לא הצליח להיכנס לא לפרודקשן ולא לדמו, מאף דפדפן. מדידה ישירה הראתה שהמסד לא מגיב: `auth/v1/authorize`, `auth/v1/settings` ו-`rest/v1/app_state` נתקעו כולם ב-12 שניות (3/3 ניסיונות), בזמן שדחיות ברמת השער חזרו ב-0.11s. כלומר **שום מסלול כניסה לא היה יכול לעבוד**.

**השורש הוא בדיוק מה שתועד ב-`DISK_IO_INVESTIGATION.md` באותו בוקר** — snapshot מלא של ~46KB נכתב למסד אחרי כל עריכה. התחזית "המופע עלול להפוך ללא-מגיב" התממשה תוך 11 שעות.

**נפרס בשתי הגרסאות** (`b063def` דמו · `cfebcde` פרודקשן): (1) snapshot לענן רק בנקודות ציון, (2) timeout של 12 שניות על כל 12 הקריאות, (3) מוצא חירום "המשך ללא חיבור" לנתונים מקומיים — בלי לגעת ב-RLS.

**אומת חי:** `node --check` עבר · פרודקשן מציג את באנר הנתק ואת הכפתור, 0 שגיאות קונסול · ההפרדה `app_state` מול `app_state_demo` נשמרה. גיבוי ונקודת rollback נוצרו לפני הנגיעה בפרודקשן.

**✅ נסגר באותו ערב:** רז ביצע restart, המסד חזר תוך 120 שניות. `app_state` → 200 · Google authorize → 302 תוך 1.7s (היה תקוע 12s) · **הנתונים שלמים** — 2 שורות משתמשים, סליקות, 20 נקודות שחזור, 1001 kB. באנר הנתק באפליקציה **נעלם מעצמו**, מה שמאמת שגילוי-הנתק עובד בשני הכיוונים.

**לקח נוסף (`L-9`):** `get_project` דיווח `ACTIVE_HEALTHY` **לאורך כל הקריסה**. דגל בריאות של ספק אינו עדות לזמינות — מה שאבחן היה השוואת זמני תגובה בין נקודת קצה שנוגעת במסד לאחת שנעצרת בשער.

> **הערת תהליך:** זו הפעם הראשונה שנגענו בפרודקשן לפי המדיניות החדשה (push ללא אישור פרטני). הכלל "דמו קודם" נשמר — התיקון רץ בדמו, אומת, ורק אז הועבר.

---

## 2026-09-10 (ג) — בדיקת דמו באייפון · באג בסנכרון "טופל" נמצא ותוקן

רז בדק את `DEMO_CHECK_LIST.md` בפועל באייפון. **3 מתוך 4 סעיפים עברו נקי.** סעיף 2 (אימות `goals` בקובץ ייצוא) הוחלף באימות קוד — `exportBackup()` בונה מ-`getBackupKeys()` שכוללת בהכרח את `goals_v1`, אין צורך בחיפוש ידני.

**סעיף 3 נכשל:** פריט שסומן "טופל" בדשבורד חזר אחרי רענון. **נמצא הגורם:** `dismissReviewItem()` (שורה 6091) כתב ל-`localStorage` אבל לא קרא ל-`markLocalDataChanged()`/`scheduleCloudSync()` כמו כל פונקציית שמירה אחרת באפליקציה — הדחיפה לענן מעולם לא קרתה, וברענון `pullCloudSnapshot()` דרס את הסימון המקומי בגרסה הישנה מהענן.

**תוקן בדמו בלבד** (שתי שורות, זהה לתבנית הקיימת בכל שאר פונקציות השמירה). תועד כ-`F-14` ב-`RESEARCH_LOG_AND_LESSONS.md`. **ממתין רטסט של סעיף 3 בלבד** מהאייפון לפני מעבר לשלב 0 (העברת 3 הקומיטים + התיקון הזה ל-production).

---

## 2026-09-10 (ב) — מיפוי מלא בחמישה חלקים · ממצא קריטי ב-production · תכנית אב

**נוצרו 7 מסמכים:** `PRODUCT_SURFACE_MAP_V2` · `DATA_MODEL_MAP` · `CALC_LOGIC_MAP` · `STATE_VS_TARGET_MAP` · `AGENTS_AND_TOOLING_MAP` · `RESEARCH_LOG_AND_LESSONS` · `MASTER_PLAN_TO_FINISHED_APP`.

**🔴 הממצא המרכזי — אובדן נתונים פעיל ב-production.** `getAllUserStorageKeys()` מכיל 11 מפתחות בדמו (שורה 2727) ורק **7** ב-`expense-app-v37.html` (שורה 2707). טאב היעדים **עובד** ב-production וכותב ל-`localStorage` (שורה 3412), אבל `expense_app_goals_v1` אינו ברשימת הסנכרון — ולכן **לא עולה לענן ולא נכנס לייצוא הגיבוי**. אותו דבר ל-`expense_app_merchant_memory_v1` (שורה 4864). בנוסף, הגנת `QuotaExceeded` מ-`d87d2c0` חסרה ב-v37 (`grep -c` → demo 1, v37 0). **המשמעות:** 3 הקומיטים שממתינים אינם שיפורי נוחות אלא תיקוני אובדן נתונים; `NA-A` עולה מ"רצוי" ל"דחוף".

**ממצאים נוספים:** 10 טאבים ולא 9 (`goals` לא היה מתועד) · 3 מפתחות `localStorage` ללא namespace · RLS פתוח ל-anon (הפרדה ב-client בלבד) · 4 חשדות בלוגיקת חישוב, הבולט — יתרת פתיחה מותנית ב-`currentUser === "raz"` (שורה 5206) ולכן שירה כנראה בלי יתרה ובלי תחזית · 2 סוכנים עם הרשאות רחבות מדי.

**תיקון עצמי:** הצהרתי ש-`design-critique` אינו קיים. **טעות** — הוא קיים, יחד עם 58 skills נוספים, ב-`~/Library/Application Support/Claude/local-agent-mode-sessions/…/rpm/plugin_*/skills/`. החיפוש שלי כיסה רק את `~/.claude` ואת הבינארי. הלקח נרשם כ-`L-3`: לפני שמצהירים ש-skill לא קיים — להריץ אותו. **מה שכן אומת בהרצה:** הפלט הוא checklist עם placeholders ולא מדידה — `L-4`.

**תיקון זיכרון:** סיכון הסליקה המשותפת (היעדר tombstones + id חלש) **נסגר** — tombstones בשורה 4585, id חזק בשורה 4570.

**סטטוס:** `WAIT`. 9 ממצאי קוד פתוחים, 5 לקחי תהליך מומשו, 4 החלטות ממתינות (`D-04`, `D-05`, `D-06`, `L-2`).

---

## 2026-09-10 — רענון בסיס: מישור הבקרה היה מנותק מהמציאות במשך 12 יום

**מה קרה:** מסמכי הבקרה עודכנו לאחרונה ב־28–29/08. מאז נעשו **29 קומיטים**, כולם נדחפו (`0 ahead / 0 behind`). המסמכים לא עודכנו איתם, וכתוצאה מכך תיארו מציאות שחדלה להתקיים.

**סתירות שנמצאו בין המסמכים למציאות:**

| מסמך | מה נכתב בו | המציאות ב-2026-09-10 |
|---|---|---|
| `PROJECT_STATUS_SNAPSHOT.json` | `production_changed: false`, `commit_created: false`, `pushed: false` | **v37 production שונה ב-8 קומיטים נפרדים**, כולם נדחפו |
| `PROJECT_OPERATING_PLAN.md` | "שחרור production — חסום במכוון" | production קיבל Google OAuth, סנכרון עיצוב מלא, סטטיסטיקות הכנסות, גרפים שנתיים וטאב חובות |
| `CLAUDE_NEXT_ACTION.md` | "הפעולה הבאה: לפרסם את הקשחת זהות הסנכרון" | בוצע ונדחף ב־`c7ece29`, לפני 12 יום |
| `DECISION_REGISTER.md` D-03 | "מתי לקדם V38 ל־production — חסום עד Gate 1–3" | הקידום **כבר קרה, בחלקים**, בלי שההחלטה נסגרה ובלי gate יחיד |

**המשמעות:** לא מדובר באי־דיוק טכני. ה־gate שנועד לחסום שחרור production לא נאכף בפועל — production התקדם דרך קומיטים נקודתיים במקום דרך ה־Launch Packet. זה הפער החשוב ביותר ברשומה הזו.

**מצב אמיתי כרגע:**
- `expense-app-v37-demo.html` — 9,221 שורות · `expense-app-v37.html` — 8,994 שורות. **הדמו מקדים ב־227 שורות.**
- 3 קומיטים מהיום **טרם הועברו ל-production**: `2e4bc93` (ריבוי פריטי בדיקה + עיצוב טופס קטגוריות + מיידי/משותף בייבוא), `18f64dc` (רישום מפתחות סנכרון חסרים + הגנה משגיאה שקטה בייבוא אשראי), `d87d2c0` (הגנה מחריגת מכסת localStorage — היה שובר את הייבוא לגמרי).

**ממצאי היגיינה שלא היו מתועדים באף מקום:**
1. **`.gitignore` מכיל `*.json`** → כל 12 גיבויי הנתונים ב־`backups/` קיימים ב־iCloud בלבד, **ללא היסטוריית גרסאות**. `CLAUDE.md` מגדיר אותם אזור סיכון גבוה ("לא למחוק, לא לשנות") — אבל git לא מגן עליהם.
2. **פקודת הגיבוי ב־`CLAUDE.md` שבורה.** היא מפנה ל־`backups/raz-expenses-backup-LATEST.json` — **קובץ בשם הזה אינו קיים**. הגיבוי האחרון בפועל הוא `raz-expenses-backup-2026-09-09_15-32.json`. כלומר הפקודה שאמורה לרוץ "בלי לשאול" לפני כל שינוי נתונים — נכשלת.
3. **`CLOUD_DATA_RELIABILITY_PLAN.md` אינו במעקב git**, למרות ש־`PROJECT_OPERATING_PLAN.md` מצטט אותו כהוכחה לאירוע P0.
4. לא במעקב git גם: `business-plan/`, `friend-setup/`, `backups/`, 3 קבצי SQL של Supabase, 5 קבצי `archive/*.html`.

**חוטים פתוחים שלא נסגרו ולא נסגרו במפורש:**
- `TWO_DEVICE_SYNC_ACCEPTANCE.md` — מטריצת הקבלה מעולם לא דווחה כמבוצעת. היא מזכירה **"the 190 ₪ discrepancy"** שאמור להיפתר או להיות מיוחס להפרש scope מתועד. אין רישום שזה קרה. `UNKNOWN` — פער כספי פתוח.
- `FEATURE_MASTER_BACKLOG.md` מצהיר על כיוון עיצוב "כהה/עמוק, electric-lime/cyan/violet, **לא 'אתר עץ'**", בעוד הקומיטים מ־28/08 הלכו דווקא לכיוון `light wood` / `light oak` / `warm tech`. `UNKNOWN` — האם הבקלוג מתאר כוונה שנזנחה, או שהעיצוב סטה מהכוונה.
- הכלל הארכיטקטוני בבקלוג — "`Personal` הוא ה-default, `Couple` משטח נפרד ולא פילטר" — לא אומת מול הקוד בסבב הזה.

**סטטוס:** מסמכי המצב (`PROJECT_STATUS_SNAPSHOT.json`, `CLAUDE_NEXT_ACTION.md`, `PROJECT_OPERATING_PLAN.md`, `DECISION_REGISTER.md`) עודכנו למציאות. מסמכי הייחוס (`FEATURE_MASTER_BACKLOG.md`, `FULL_PRODUCT_SURFACE_MAP.md`, `EXPENSE_APP_V38_LAUNCH_PACKET.md`, `IMPORT_AUTOMATION_PLAN.md`, `HOUSEHOLD_AUTHORIZATION_DESIGN.md`) **לא נגעתי בהם** — הם דורשים הכרעה שלך, לא עדכון טכני.

---

## 2026-08-28 — V38 demo implementation

- נלמדה ערכת `PROJECT_CONTROL_STARTER_KIT` והוגדרו launch packet, owner, writer, risk, rollback ו־gates.
- הושלם מיפוי של תשעת הטאבים, כל טבלאות/רשימות, סינונים, מצבי עריכה, מודאלים וכתיבות נתונים.
- נלכדו screenshots של כל הזרימות המרכזיות ב־demo הפרוס לפני השינוי.
- נבחר כיוון “Safety & Automation” והותאם לעץ בהיר, ירוק מרווה וטקסט אגוז כהה.
- עודכנה מעטפת demo בלבד: מצב חיבור, נתוני בן/בת זוג, dashboard monthly status, review queue ומצב עריכה גלובלי.
- יושרה השפה החזותית בכל הטאבים, הטפסים, הטבלאות, ה־filters, modal/sheet, empty/error/sync states.
- תוקנו `aria-selected`, מצב “עוד”, הופעת FAB רק בטאב הוצאות ו־overflow של חובות במובייל.
- שופר ייבוא אשראי: review summary, fingerprint, recovery point ומידע מקור על העסקה.
- נוסף שער כניסה לדמו; לאחר התחברות זהות העריכה ננעלת לפי `app_user_key`.
- תועדה חסימת RLS: צפייה זוגית מלאה דורשת household policy בשרת, ולא שינוי דפדפן בלבד.
- בדיקת תחביר demo עברה.
- בדיקה חיה/אייפון והשוואת design QA ממתינות לאישור commit+push; production לא השתנה.
