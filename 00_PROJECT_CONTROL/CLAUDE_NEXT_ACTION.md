# הפעולה הבאה

> **עדכון 15.09.2026.** ר' `STATUS_CHANGELOG.md` (2026-09-14, שני הרשומות מ-"ה") ו-`DECISION_REGISTER.md` D-08/D-04/D-12 לפני שממשיכים — התוכן למטה הוא תמצית, לא תחליף.

## ⛔ שלושה חסמים לפני עבודה חדשה, לפי סדר

**1. בדיקה במכשיר.** גלים 1–3.5 של DATA-04 יושבים בדמו ללא בדיקה אחת באייפון. `production` מפגר ומריץ באגים שכבר תוקנו בדמו. רשימת הבדיקה: הכפתור הלבן "עדכן יתרה מהבנק" · מסך הזנה עם מקלדת ספרתית · הדבקת יתרה מהבנק מחלצת מספר נכון · שמירה מציגה "תואם"/פער · הבאנר נשאר · sparkline לא צונח בינואר · תאריכים לא הפוכים.

**2. תיקון D-08 המצומצם — ✅ מומש בדמו 15.09, נמדד, לא מקודם.**
דוח הביקורת: [`D08_NARROW_FIX_REVIEW_2026-09-15.md`](D08_NARROW_FIX_REVIEW_2026-09-15.md). תמצית + מה תוקן: D-08 במרשם ההחלטות.

- **שלוש המלכודות שהביקורת מצאה — תוקנו:** `parseDashedStatementDate()` ייעודית (לא `toIsoDate` על פורמט שהוא לא תומך בו); שדה `billingDateRaw` per-row בענף "max"; `ensureSheetExists` נקרא בייבוא כמו בהוספה ידנית.
- **מלכודת אחת נשארה במכוון:** בדיקת חודש-סגור לא נוספה כי גל 4a (MONTH_CLOSE_SPEC) לא קיים בקוד — אין מה לבדוק מולו. **אזהרה בקוד עצמו** (מעל `confirmCCImport`) מחייבת לחזור לכאן כשגל 4a נכתב.
- **נמדד:** 17/17 עסקאות מזרחי אמיתיות → `billingDateRaw` תקין → ניתוב ל-**יולי 26** (לפני התיקון: יוני 26, לכולן). **0 שורות ידניות זזו** (599 אצל רז + 272 אצל שירה, מול הקוד שלפני התיקון). `balance_harness --selftest` 19/19.
- **כלי אימות ב-`tools/` (שודרגו ל-assert+exit-code):**
  - `node tools/cc_import_probe.js expense-app-v37-demo.html <statement.xlsx> [xlsx-lib-path]` — דורש `curl -sL "https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js" -o /tmp/xlsx.full.min.js` קודם, אישור מפורש בכל הרצה.
  - `node tools/billing_cycle_gate_check.js <app.html> <backup.json> [user] --out <path>` לשמירת baseline, `--baseline <path>` להשוואה — `PASS`/`FAIL` עם exit code, לא רק הדפסה.
- **עדיין לא נבדק:** ייבוא כפול בפועל בדמו (לוודא dedup חי, לא רק בקוד) לפני קידום ל-production. **לא להפוך את כלל הנפילה-לאחור בהזנה ידנית** — זה עדיין גייט נפרד, לא נגעו בו (ר' D-08).

**3. D-11 (בסיס תצוגת מחזור)** — תלוי בסעיף 2 לעיל. לא לפתוח לפני שהוא נסגר.

## אחרי שלושת אלה

- **עיצוב מחדש** — חוזי השימור מ-14.09, המשך למפרט חזותי מדוד. אסור לחזור לשאלת כיוון בסיסי.
- **GROC** — רשימת סופר, ר' עדכונים למטה.
- **`AccountActivity.xls` (עו״ש)** — נחקר ב-14.09 (HTML כ-.xls, יתרה מתוארכת + עמודת יתרה רצה), **לא תוכנן מודל צריכה**. שונה מהותית מייבוא כרטיס אשראי — לא לחבר לצינור `ccImport` הקיים בלי החלטה נפרדת על מניעת ספירה כפולה.

---

## מסלול העיצוב

רז אישר את הכיוון החזותי וביקש לשמר כל יכולת ונתון. [חוזי השימור מ־14.09](../design/specification/contracts-2026-09-14/README.md) הם ההמשך הפעיל למטריצת 57 היכולות.

בוצע: שיוך 151 פקדים פתוחים; יעדים לכל 506 מופעי תגיות; 17 קבוצות טפסים ו־71 שדות לוגיים; 35 שדות כלי שכר; 39 הגדרות מדדים/עמודות; מידע רקע, מצבי עבודה ותרחישי קבלה. אין עדיין כיסוי חזותי או runtime מלא.

המשך: להמיר את חוזה המסכים והזרימות למפרט חזותי מדוד, להשלים פלטים דינמיים וכללי null/אפס לכל מדד, ולבדוק גרסה חיה וזרימות מחוברות. אסור לחזור לשאלה איזה כיוון בסיסי לבחור, או להשמיט מסלולים שלא הופיעו בארבע התמונות.

## בקשת מחקר נוספת — רשימת סופר (14.09)

לקרוא [מחקר הקניות](../research/2026-09-14/grocery/GROCERY_RESEARCH_HE.md) ולשלב את GROC-01–06 מהבקלוג. מדובר בהרחבה חדשה, לא ביכולת קיימת שנסקרה במטריצת השימור. השלב הבא המוצע: רשימת הפתקים והסניפים → מפרט רשימה ומצב קנייה; לא להוסיף ספק OCR/שליחת קבלות/התראות אמיתיות כחלק סמוי מהמחקר. הבקשה אינה מבטלת את התכנון ושימור היכולות הקיימים.

### עדכון GROC — הרשימה התקבלה

לקרוא את [הניתוח האישי](../data/private-grocery/PERSONAL_LIST_REVIEW.md) ואת catalog.json שלצדו: 134 שורות מקור, 132 שמות שונים, שלושה תיקוני שם מאושרים ושתי קבוצות כפילות טקסטואלית שלא מוזגו. הרשימה היא קטלוג קבוע ולא רשימת חוסרים פעילה. קבצי המקור והקטלוג פרטיים ומוחרגים מ־git; אין להעתיקם לנכסי אתר או לדמו פומבי. ההמשך: קטלוג → בחירה לקנייה נוכחית, בירור סניפים וזיהוי מוצרים הדרגתי; להמשיך באותו כיוון עיצוב.

### עדכון GROC — הרשתות העיקריות אושרו

נשמרו חמש הרשתות העיקריות שמסר המשתמש ב[העדפות הקנייה הפרטיות](../data/private-grocery/store-preferences.json). אלה רשתות עיקריות ולא רשימה בלעדית או דירוג. אזור הקנייה וסניף נוסף נמסרו ונשמרו בקובץ הפרטי; נותרו זיהוי הסניפים המדויקים בארבע הרשתות האחרות, התאמה למזהי סניף רשמיים וערוץ קנייה; אין להניח מחיר אחיד ברשת או חברות במועדון. מחברי מחירים עדיין לא נבדקו בפועל.
