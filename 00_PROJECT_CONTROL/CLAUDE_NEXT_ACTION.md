# Next Action

**הפעולה הבאה:** לפרסם את הקשחת זהות הסנכרון (זהות Supabase נפרדת מבורר התצוגה) ולבצע את `TWO_DEVICE_SYNC_ACCEPTANCE.md` על שני המכשירים, עם פריט בדיקה מתויג.

**ממצא נוכחי:** לאחר תיקון קריאת ה־RLS נמצאה גם ערבוב זהויות: `currentUser` שימש גם לבורר UI וגם ליעד סנכרון. הקשחת הזהות מפרידה את הפעולות הקריטיות לפי `app_user_key` המחובר ומפצלת חותמות שינוי לפי משתמש.

**DoD:** שני מכשירים רואים אותו build, סשן שירה מזוהה, ותסריטי Raz/Shira/unauthenticated לקריאה/כתיבה/מחיקה, rollback ונתון פרטי מתועדים ומאושרים.
