# הפעולה הבאה

> **עדכון 14.09.2026 — הפעולה הבאה בפועל.** הקובץ הזה תיאר עד כה רק את מסלול העיצוב, בעוד שני סשנים מלאים הלכו ל-DATA-04 (עוגן יתרת עו״ש). זו הפרה של הכלל ב-`CLAUDE.md` — "עדכן את הפעולה הבאה… אין ליצור תוכנית מקבילה" — והיא תוקנה כאן. **שלושה מסלולים פעילים, והסדר ביניהם נקבע למטה.**

## ⛔ שני חסמים לפני כל עבודה חדשה

**1. בדיקה במכשיר.** גלים 1–3.5 של DATA-04 יושבים בדמו ללא בדיקה אחת באייפון. `production` מפגר ב-707 שורות ומריץ באגים שכבר תוקנו בדמו — אין לו `resolveSheetName` (CARRY-16: שרשרת החודשים נשברת באוקטובר 2025), ה-sparkline שלו עדיין על `cumulativeBalance` (CARRY-15), ואין ניכוי הלוואה בגרף התחזית (~948 ₪). **הערך כבר נבנה ויושב במקום הלא נכון.**

רשימת הבדיקה בדמו: המספר הגדול והכפתור הלבן "עדכן יתרה מהבנק" · לחיצה פותחת מסך הזנה עם מקלדת ספרתית · הדבקת יתרה מאפליקציית הבנק מחלצת את המספר הנכון · שמירה מציגה "תואם" או פער · באנר הפער אינו נעלם · ה-sparkline אינו צונח בינואר · תאריכים אינם הפוכים.

**2. D-04 חוזרת לרז.** ההחלטה שהתקבלה ב-14.09 (להכניס גיבויים ל-git) **בוטלה לפני ביצוע**: הנימוק "הריפו פרטי" שגוי, `visibility=PUBLIC`. ר' התיקון העובדתי במרשם ההחלטות. אומת שאין דליפה קיימת.

## סדר המסלולים (הכרעת רז, 14.09)

1. **בדיקה במכשיר → קידום ל-production** דרך `promote_demo_to_prod.py`.
2. **DATA-04 גל 4 = D-11** — בסיס תצוגת המחזור (קלנדרי מול יום-חיוב, כהעדפת משתמש). לא חסום עוד: העוגן קיים. ⚠️ תלוי ב-D-08/CARRY-08, שהגייט שלו לא מומש — **לפתוח חודש סגור אמיתי בגיבוי ולראות איך נרשם בפועל, לפני שינוי כלל `cycleDay`.**
3. **עיצוב מחדש** — המשך החוזים למפרט חזותי מדוד (הסעיף למטה).
4. **GROC** — רשימת סופר.

---

## מסלול העיצוב

רז אישר את הכיוון החזותי וביקש לשמר כל יכולת ונתון. [חוזי השימור מ־14.09](../design/specification/contracts-2026-09-14/README.md) הם ההמשך הפעיל למטריצת 57 היכולות.

בוצע: שיוך 151 פקדים פתוחים; יעדים לכל 506 מופעי תגיות; 17 קבוצות טפסים ו־71 שדות לוגיים; 35 שדות כלי שכר; 39 הגדרות מדדים/עמודות; מידע רקע, מצבי עבודה ותרחישי קבלה. אין עדיין כיסוי חזותי או runtime מלא.

המשך: להמיר את חוזה המסכים והזרימות למפרט חזותי מדוד, להשלים פלטים דינמיים וכללי null/אפס לכל מדד, ולבדוק גרסה חיה וזרימות מחוברות. אסור לחזור לשאלה איזה כיוון בסיסי לבחור, או להשמיט מסלולים שלא הופיעו בארבע התמונות.

## בקשת מחקר נוספת — רשימת סופר (14.09)

לקרוא [מחקר הקניות](../research/2026-09-14/grocery/GROCERY_RESEARCH_HE.md) ולשלב את GROC-01–06 מהבקלוג. מדובר בהרחבה חדשה, לא ביכולת קיימת שנסקרה במטריצת השימור. השלב הבא המוצע: רשימת הפתקים והסניפים → מפרט רשימה ומצב קנייה; לא להוסיף ספק OCR/שליחת קבלות/התראות אמיתיות כחלק סמוי מהמחקר. הבקשה אינה מבטלת את התכנון ושימור היכולות הקיימים.

### עדכון GROC — הרשימה התקבלה

לקרוא את [הניתוח האישי](../data/private-grocery/PERSONAL_LIST_REVIEW.md) ואת catalog.json שלצדו: 134 שורות מקור, 132 שמות שונים, שלושה תיקוני שם מאושרים ושתי קבוצות כפילות טקסטואלית שלא מוזגו. הרשימה היא קטלוג קבוע ולא רשימת חוסרים פעילה. קבצי המקור והקטלוג פרטיים ומוחרגים מ־git; אין להעתיקם לנכסי אתר או לדמו פומבי. ההמשך: קטלוג → בחירה לקנייה נוכחית, בירור סניפים וזיהוי מוצרים הדרגתי; להמשיך באותו כיוון עיצוב.
