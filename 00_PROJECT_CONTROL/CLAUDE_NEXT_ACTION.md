# Next Action

עודכן: 2026-09-13

## Status

```
GO — C-2 (ריבית) הבא בתור לאימות, האחרון משלושת החשדות המקוריים. שלב 6 (עיצוב 10 מסכים) עדיין פתוח, לא חוסם.
```

---

## מה נסגר (13/09)

| | ראיה |
|---|---|
| **C-3 + C-5** — ספירה כפולה בתחזית | חי היה ב**production**, לא רק דמו. תוקן + אומת חי: תחזית -13,894→-6,048, גרף +769→-5,230 (אותו סימן). סקירת `domain-risk-reviewer` לפני יישום |
| **C-4** — dedup חוב לאבא | ההתנגשות (23/03/2026, 1,000 ₪) נמצאה חיה ב-production בפועל. נשאל רז ישירות — **אותה העברה, המיזוג נכון**. לא שונה סכום; נוספה שקיפות (תג "🔗 מוזג עם") כדי שמיזוג שגוי עתידי יהיה נראה, לא שקט |
| טופס עריכת הוצאה בנייד | עיצוב מחדש: סכום+עסק למעלה, קטגוריה כצ'יפים (44px), פרטים נוספים מקופלים |
| לשונית גיבוי | כפתור קאשה יחיד, נקודות שחזור מקופלות, בוטלו כפתורי pull/push/disconnect (סנכרון אוטומטי כבר קיים) |
| אזהרת ייבוא + גיבוי שבועי | `describeImportImpact` מציג תאריך/היקף/התנגשות לפני שמחליפים נתונים; `maybeRunWeeklyBackup` רץ ברקע כל 7 ימים |
| אינדיקטור סנכרון | מצב "מסנכרן…" חדש (נקודה כתומה פועמת) — היה קיים רק disabled/needs-login/error/connected |

## מה נסגר (12/09, לתיעוד)

| | ראיה |
|---|---|
| **שלב 2ב** — מסלול הייבוא | F-17/18/19 נפרסו. 8 שורות של שירה: יולי 2 · אוגוסט 6 · 1,550.47 |
| **שלב 5** — מערכת עיצוב | 107 משתנים · 5 רכיבים · 30 וריאנטים · **0 ערכים מוקשחים** |
| `D-05` `D-08` `D-09` | כיוון ג׳ · חודש חיוב · pdf.js עצל |
| `C-1` | **הופרך** — רז בדק, המאזן והתחזית של שירה תקינים |

---

## הפעולה הבאה — NA-E · אימות C-2

**לאמת עם מספרים אמיתיים את C-2** (מ-CALC_LOGIC_MAP.md §2): `getDisplayedClosingBalance` מנכה מהמאזן רק את **קרן** ההלוואה החודשית (`getLoanScheduleEntryForSheet(sheet)?.principal`), לא ריבית. אם התשלום בפועל לבנק כולל ריבית שלא נרשמת בנפרד כשורת הוצאה — היא חסרה מהמאזן. **לא נבדק עדיין** (בניגוד ל-C-3/C-4 שהיו חשדות מנומקים, זה מסומן `UNKNOWN`).

**שלב ראשון לפני כל תיקון:** לבדוק אם `loanSchedule` בפועל (בנתוני production) כולל שדה ריבית נפרד או רק קרן — אם התשלום החודשי האמיתי לבנק תמיד שווה בדיוק ל-`principal`, זה לא באג, וזה נסגר מיד. אותו תהליך: תרחיש בדיקה חי → מספרים אמיתיים → אם מאומת, `domain-risk-reviewer` לפני תיקון.

**זהו החשד השלישי והאחרון מתוך C-1/C-2/C-3/C-4 המקוריים** (C-1 הופרך, C-3+C-5 תוקנו, C-4 אומת כתקין+שקיפות נוספה). אחרי C-2, "שלב 2 — אמת בנתונים" נסגר לגמרי.

**שלב 6 (עיצוב 10 מסכים) נשאר בתור, לא ננטש** — אבל דורש מבנה עבודה שונה (מסך אחד → אישור רז → הבא), שלא מתאים לעבודה הרציפה-אוטונומית המבוקשת כרגע. לחזור אליו כשרז מבקש session ממוקד עיצוב.

---

## מה שפתוח ולא חוסם

| | סוג | הערה |
|---|---|---|
| שלב 1 | היגיינה | `D-04` גיבויים ב-git · הרשאות סוכנים · 3 מפתחות ללא namespace |
| שלב 2 | אימות | חשד חישוב אחד נותר (היו 3): C-2 ריבית (גם משפיע על גרף התחזית — ר' C-5) |
| `C-6` | תצוגה | הוצאת "חוב לאבא" מוצגת גם בכרטיסיית הוצאות וגם בכרטיסיית חוב בדשבורד. לא נוגע במאזן |
| `R-04` | סיכון | `cycleDay` = תחילת מחזור בקוד, יום חיוב בדף. **לא נגעתי** — מיגרציה של ~880 שורות |
| אייקונים · density | מערכת עיצוב | Tab item עם placeholder עגול |
| iPhone verification | תזכורת | שינויי 13/09 (טופס עריכה, לשונית גיבוי, תחזית, תג מיזוג חוב לאבא) עדיין לא אושרו בפועל על מכשיר של רז/שירה |

---

## אסור

- לגעת ב-`expense-app-v37.html` בלי מעבר דרך `promote_demo_to_prod.py`
- `force push` / `reset --hard` / מחיקת branch ללא אישור מפורש
- לתקן חשד חישוב בלי מספרים אמיתיים + `domain-risk-reviewer` — **C-1 הוכיח למה, C-3/C-5 הוכיחו שוב שהתהליך עובד**
- לקדם תיקון לצרכן אחד של נתון בלי לבדוק צרכנים נוספים של אותו נתון — **L-12: כך התגלה C-5**

## בסיום כל שלב

עדכון `STATUS_CHANGELOG.md` (שורה בראש) + `PROJECT_STATUS_SNAPSHOT.json` **באותו סבב**.
`L-1` חזר עליי ב-12/09: ה-snapshot פיגר ב-40 קומיטים עד שרז שאל. נסגר אז, וסונכרן שוב היום (13/09) יחד עם C-3/C-5.
