# הפעולה הבאה

> **עודכן 18.09.2026 (מאוחר מאוד).** המקור החי: `DECISION_REGISTER.md` § "חושבין מחדש" (18.09 מאוחר) — **מחליף את D-13, U-4, D-08 שתועדו למטה**. אלה נשמרים כתיעוד היסטורי של החלטות שבוטלו, לא כמצב פעיל.

## 🔴 העדכון המשמעותי ביותר: "חושבין מחדש" — מודל חודש-חיוב נגזר בוטל

רז: *"נהיה פה סבוכה עצומה של חישובים... אני רוצה שנעשה חושבין מחדש."* כל מודל "חודש חיוב" הנפרד מהגיליון (D-13, U-4, מועדף-תאריך-בנק ב-D-08) הוסר. כלל יחיד: כרטיס מיידי = חודש קלנדרי תמיד; אשראי רגיל = יום<מחזור מוזז אחורה. הגיליון הוא הבסיס היחיד לכל מסך. **מומש ומאומת בדמו, לא קודם לפרודקשן.** פירוט מלא + before/after + מה שנשאר פתוח: `DECISION_REGISTER.md` § "18.09.2026 (מאוחר) — חושבין מחדש".

**מה שנשאר לפני קידום:**
1. רז מסיים לעדכן הוצאות בפרודקשן (בעבודה כרגע).
2. תיקון נתוני פרודקשן: 15 שורות מייבוא שגוי (ויזה מזרחי, batchId `cc-1789751570167-8sp5ut`) יושבות באוקטובר; לפי הכלל החדש 5 שייכות לאוגוסט ו-10 לספטמבר.
3. רז בודק דמו במכשיר → קידום.
4. F-20/F-21 (ייבוא: כרטיסים מרובים בקובץ, גיליונות עסקאות-מיידיות/טרם-נקלטו שלא נקראים) — פתוחים, לא חוסמים.

## מדיניות קידום (רז, 18.09, מאוחר): פרודקשן תמיד תקין

רז: *"אני רוצה שהפרודקשן תמיד יהיה אידיאלי לשימוש ולצפייה בנתונים... התיקונים... נשארים בדמו עד שהם לא הורסים שום דבר."* המשמעות: **תיקוני נכונות מקודמים ברגע שהם מאומתים, בנפרד מעבודת פישוט/עיצוב.** ר' `DECISION_REGISTER.md` לניסוח המלא.

## סדר העדיפויות קדימה (עודכן 18.09 מאוחר)

| # | פריט | סטטוס |
|---|---|---|
| 1 | **C-8** — להחזיר את תזרים ההלוואה ל-`.principal` (#42) | ✅ **מומש ומאומת בדמו** — 10 מקומות תוקנו, `cash_out_probe`+`consistency_audit_probe` ירוקים. ר' `DECISION_REGISTER.md` § C-8. |
| 2 | **U-7** — לפשט את הדשבורד (#41) | ✅ **מומש ומאומת חי בדמו** (Playwright) — כותרת יחידה + קיפול "פרטים נוספים", כפילות הוסרה, באג בסיס-חישוב בצ'יפ ההפרש תוקן. ר' `STATUS_CHANGELOG.md`. |
| 3 | **בדיקת רז באייפון** על הדמו (חישוב + עיצוב יחד), ואז **קידום ל-v37** | ⏳ ממתין לבדיקת מכשיר — זה השלב היחיד שנשאר. |
| 4 | **בפרודקשן בלבד:** ייבוא דוח בנק אמיתי דרך המסך, כדי שלספטמבר יהיה בכלל עוגן | ⏳ פעולת נתונים של רז, נפרדת מקידום קוד — בלעדיה גם אחרי הקידום ספטמבר יציג "—" בפרודקשן. |
| 5 | #35 — מעבר מלא לבסיס תזרימי | נדחה עד שיצטברו עוד חודשים נקיים. |

**מה זה אומר בפועל:** נבדק חי (Playwright, לא רק קוד) — פרודקשן היום לא רק "שגוי", הוא **ריק** לספטמבר: "לא הוגדר עו״ש פתיחה לחודש הזה — אי אפשר לחשב יתרה". זה נפרד מתיקון הקוד: גם אחרי שהחבילה תקודם, ספטמבר בפרודקשן יישאר "—" עד שרז יבצע שם ייבוא דוח בנק אמיתי (כמו שכבר נעשה בדמו).

---

## 🔴 אחרי C-8 — U-7: לפשט את הדשבורד

**רז, 18.09, בסוף הסשן:** *"עשינו בלאגן גבר, אני לא רוצה את הסיבוך הזה, משתמש בחיים לא יבין מה הוא קורא. בוא נחקור שנייה על לפשט את זה."*

הסשן של 18.09 תיקן ארבעה באגים אמיתיים בחישוב ובתצוגה (U-1..U-6, כולם מאומתים מול דוח בנק אמיתי) — **והמחיר היה צפיפות**. הכותרת נושאת היום כ-13 יחידות מידע: מספר ענק, תווית, כפתור עוגן, שורת מעוגן/נרשם, צ'יפ אחוזים, שורת תחזית, שלושה תאי תזרים, תת-תווית תאריך, שלושה מדדי חודש, שורת מגמה וצ'יפ חוב. בנוסף "עו״ש עכשיו" בתא הימני הוא בדיוק אותו מספר כמו הכותרת הגדולה — פעמיים אותו נתון.

**חשוב:** זו **עבודת תצוגה בלבד**. החישוב תחתיה נבדק ואומת מול הבנק; אין סיבה לגעת בו. כל ניסיון "לפשט" שמשנה מספר — עצור ושאל.

כיווני חקירה (הצעות, לא החלטות): לאחד את הכותרת עם "עו״ש עכשיו"; להחליט אם המספר הראשי צריך להיות "כמה יש" או "כמה באמת שלי אחרי חיובי אשראי"; לקפל את שורת המעוגן והצ'יפים מאחורי לחיצה; לקצר את הבאנר לשורה אחת עם "הסבר".

**מצב הקוד:** הכל בדמו בלבד, לא קודם ל-v37. קומיטים `c8db9f3`, `e5acafd`, `8ea5589`, `19b1d53`.

## מצב נוכחי — שלושת החסמים של 15.09 נסגרו

**1. בדיקה במכשיר — ✅ נסגר 16.09.** רז בדק ידנית באייפון ואישר; הדמו קודם ל-`production` בקומיט `1fdc339` דרך `promote_demo_to_prod.py`. `production` כבר אינו מפגר.

**2. D-08 — ✅ נסגר.** נמדד ב-Node ובדפדפן חי (15.09), והמדידה על קובץ הבנק האמיתי (16.09) אישרה את יום החיוב בפועל. הפירוט ההיסטורי נשמר למטה. נותר פתוח בנפרד: כלל הנפילה-לאחור בהזנה ידנית.

**3. D-11 — 🔄 הוחלף.** ב-16.09 רז הכריע על בסיס קלנדרי אחיד לכולם. הפריט הפעיל הוא **D-13** (חודש קלנדרי אחיד + חודש-חיוב נגזר + הצלבה מול עו״ש).

### מה פתוח עכשיו ב-D-13

- **שלב 4 — חודש קלנדרי בדיווח הכללי: לא התחיל.** נדרשת שיחת scoping לפני התחלה: בניגוד לכל מה שנבנה עד כה (קריאה-בלבד, תוספתי) זה **משנה מספרים קיימים** בתקציב, בדוח השנתי ובתחזית.
- **שלב 5 המלא — מסך הצלבה ייעודי.** מה שקיים היום הוא כרטיס סיכום בתוך טאב אשראיים, לא מסך.
- **CARRY-15** — התיקון ממתין לשורת המשכורת האמיתית של רז, שחסרת תאריך.
- **מקס חול** — 0/13 חודשים תואמים; דורש בדיקה מול הבנק, לא קוד.

---

### תיעוד היסטורי — פירוט D-08 מ-15.09

**תיקון D-08 המצומצם — ✅ מומש בדמו 15.09, נמדד ב-Node **וב-דפדפן בפועל**, לא מקודם.**
דוח הביקורת: [`D08_NARROW_FIX_REVIEW_2026-09-15.md`](D08_NARROW_FIX_REVIEW_2026-09-15.md). תמצית + מה תוקן: D-08 במרשם ההחלטות (שתי רשומות 15.09 — תיקון הקוד, ואז הבדיקה החיה).

- **שלוש המלכודות שהביקורת מצאה — תוקנו:** `parseDashedStatementDate()` ייעודית; שדה `billingDateRaw` per-row בענף "max"; `ensureSheetExists` נקרא בייבוא.
- **מלכודת אחת נשארה במכוון:** בדיקת חודש-סגור לא נוספה — גל 4a (MONTH_CLOSE_SPEC) לא קיים בקוד. אזהרה בקוד עצמו מעל `confirmCCImport`.
- **נמדד ב-Node:** 17/17 עסקאות מזרחי → `billingDateRaw` תקין → ניתוב ל-יולי 26. 0 שורות ידניות זזו (599+272). `balance_harness --selftest` 19/19.
- **✅ נמדד בדפדפן החי (15.09, מאוחר יותר):** קובץ **שני**, שונה (כרטיס Max, 4 עסקאות דצמבר–מאי) → הממשק הציג "← יוני 26" לכולן, תואם לתאריך המוצהר. ייבוא אושר בפועל. **אותו קובץ הועלה שוב** → "כפילויות שסומנו: 4, נבחרו: 0" — dedup עבד live, לא רק בקוד. סעיף "עדיין לא נבדק" נסגר.
- **כלי אימות ב-`tools/` (assert+exit-code):**
  - `node tools/cc_import_probe.js expense-app-v37-demo.html <statement.xlsx> [xlsx-lib-path]`
  - `node tools/billing_cycle_gate_check.js <app.html> <backup.json> [user] --out/--baseline <path>`
- **לא לגעת בלי דיון נפרד:** כלל הנפילה-לאחור בהזנה ידנית — גייט נפרד, לא נסגר (ר' D-08).

(D-11 הוחלף ב-D-13 ב-16.09 — ר' למעלה.)

## נמצא תוך כדי (18.09 מאוחר) — לא חוסם, ממתין בתור

- **F-20** (#43) — ייבוא Max עם 2+ כרטיסים בקובץ אחד משייך הכל לכרטיס הראשון שזוהה. אומת בקוד. ר' `DECISION_REGISTER.md`.
- **F-21** (#44) — טופס ייבוא אשראי לא מציע עסקאות מיידיות/טרם-נקלטו. דיווח רז, טרם אומת מול קובץ אמיתי — דורש קובץ Max עם עסקאות כאלה לפני שמנחשים תיקון.

## מסלולים נוספים

- **עיצוב מחדש** — חוזי השימור מ-14.09, המשך למפרט חזותי מדוד. אסור לחזור לשאלת כיוון בסיסי.
- **GROC** — רשימת סופר, ר' עדכונים למטה.
- **`AccountActivity.xls` (עו״ש)** — נחקר ב-14.09 (HTML כ-.xls, יתרה מתוארכת + עמודת יתרה רצה), **לא תוכנן מודל צריכה**. שונה מהותית מייבוא כרטיס אשראי — לא לחבר לצינור `ccImport` הקיים בלי החלטה נפרדת על מניעת ספירה כפולה.

---

## מסלול העיצוב

רז אישר את הכיוון החזותי וביקש לשמר כל יכולת ונתון. [חוזי השימור מ־14.09](../design/specification/contracts-2026-09-14/README.md) הם ההמשך הפעיל למטריצת 57 היכולות.

בוצע: שיוך 151 פקדים פתוחים; יעדים לכל 506 מופעי תגיות; 17 קבוצות טפסים ו־71 שדות לוגיים; 35 שדות כלי שכר; 39 הגדרות מדדים/עמודות; מידע רקע, מצבי עבודה ותרחישי קבלה. אין עדיין כיסוי חזותי או runtime מלא.

המשך: להמיר את חוזה המסכים והזרימות למפרט חזותי מדוד, להשלים פלטים דינמיים וכללי null/אפס לכל מדד, ולבדוק גרסה חיה וזרימות מחוברות. אסור לחזור לשאלה איזה כיוון בסיסי לבחור, או להשמיט מסלולים שלא הופיעו בארבע התמונות.

## בקשת מחקר נוספת — רשימת סופר (14.09)

לקרוא [מחקר הקניות](../research/2026-09-14/grocery/GROCERY_RESEARCH_HE.md) ולשלב את GROC-01–06 מהבקלוג. מדובר בהרחבה חדשה, לא ביכולת קיימת שנסקרה במטריצת השימור. השלב הבא המוצע: רשימת הפתקים והסניפים → מפרט רשימה ומצב קנייה; לא להוסיף ספק OCR/שליחת קבלות/התראות אמיתיות כחלק סמוי מהמחקר. הבקשה אינה מבטלת את התכנון ושימור היכולות הקיימים.

### עדכון GROC — הרשימה התקבלה

לקרוא את [הניתוח האישי](../data/private-grocery/PERSONAL_LIST_REVIEW.md) ואת catalog.json שלצדו: 134 שורות מקור, 132 שמות שונים, שלושה תיקוני שם מאושרים ושתי קבוצות כפילות טקסטואלית שלא מוזגו. הרשימה היא קטלוג קבוע ולא רשימת חוסרים פעילה. קבצי המקור והקטלוג פרטיים ומוחרגים מ־git; אין להעתיקם לנכסי אתר או לדמו פומבי. ההמשך: קטלוג → בחירה לקנייה נוכחית, בירור סניפים וזיהוי מוצרים הדרגתי; להמשיך באותו כיוון עיצוב.

### עדכון GROC — הרשתות העיקריות אושרו

נשמרו חמש הרשתות העיקריות שמסר המשתמש ב[העדפות הקנייה הפרטיות](../data/private-grocery/store-preferences.json). אלה רשתות עיקריות ולא רשימה בלעדית או דירוג. אזור הקנייה וסניף נוסף נמסרו ונשמרו בקובץ הפרטי; נותרו זיהוי הסניפים המדויקים בארבע הרשתות האחרות, התאמה למזהי סניף רשמיים וערוץ קנייה; אין להניח מחיר אחיד ברשת או חברות במועדון. מחברי מחירים עדיין לא נבדקו בפועל.
