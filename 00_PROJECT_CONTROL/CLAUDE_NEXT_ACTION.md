# Next Action

עודכן: 2026-09-12

## Status

```
GO — שלב 2ב, מסלול הייבוא. אין חוסם.
```

> **עודכן 2026-09-12.** שלב 0 **נפרס**. רז דיווח על שלוש תקלות בייבוא (`F-17`/`F-18`/`F-19`),
> כולן אומתו בקוד. `D-08` ו-`D-09` נסגרו, כך שאין הכרעה פתוחה שחוסמת.
>
> **סדר חובה:** `F-17` (מחזור) → `F-19` (אמריקן אקספרס) → `F-18` (קבועה/משתנה).
> הקובץ של שירה פרוס על 31/07–07/09; תמיכת PDF לפני תיקון המחזור תשכפל את הבאג על נתונים חדשים.
>
> **🔴 עצירה מובנית בתוך 2ב** — לפני כתיבת קוד:
> 1. `domain-risk-reviewer` על אי-ההתאמה הסמנטית ב-`cycleDay` (ר' `DECISION_REGISTER.md` → D-08)
> 2. לפתוח **חודש סגור קיים** ולראות איך הוצאות נרשמו שם בפועל — אם ההיסטוריה נרשמה
>    לפי הפרשנות הישנה, שינוי הכלל בלי מיגרציה **יזיז מספרים אחורה**
>
> **Figma ממתין בכוונה:** Phase 1 הושלם (91 משתנים). Phase 2 יחודש אחרי 2ב —
> לא מעצבים מעל נתון שיושב בחודש הלא נכון.

> **עודכן 2026-09-10 (ב):** המיפוי המלא הפך את `NA-A` מ"רצוי" ל**דחוף**. הוכח ש-4 מפתחות
> אינם מסונכרנים ב-production — כולל יעדים וזיכרון עסק — ושהגנת מכסת `localStorage` חסרה שם.
> **`NA-B` (היגיינת נתונים) נדחה לשלב 1.** ר' `STATE_VS_TARGET_MAP.md` ו-`RESEARCH_LOG_AND_LESSONS.md` F-1/F-2.
>
> **עודכן 2026-09-10 (ג):** רז בדק את הדמו באייפון — 3/4 סעיפים עברו. סעיף 3 נכשל
> (פריט "טופל" חוזר אחרי רענון), הגורם נמצא ותוקן בדמו (`F-14` — `dismissReviewItem()`
> לא סינכרן לענן). **התיקון מוסיף רביעי קומיט לרשימת ההעברה ל-production.**
>
> **החוסם היחיד:** אישור שלך שרטסטת רק את סעיף 3 בדמו מהאייפון — לסמן פריט כטופל,
> לרענן, לוודא שהוא לא חוזר. שאר הצ'קליסט כבר אושר.

---

## ✅ הושלם — מה שהיה כתוב כאן קודם

הפעולה הקודמת ("לפרסם את הקשחת זהות הסנכרון") **בוצעה ונדחפה** בקומיט `c7ece29` ב־2026-08-29. הקובץ הזה נשאר תקוע עליה 12 יום.

**מה שלא הושלם ממנה:** מטריצת `TWO_DEVICE_SYNC_ACCEPTANCE.md` על שני המכשירים מעולם לא דווחה כמבוצעת, כולל סעיף **הפרש 190 ₪** שנדרש להיסגר או להיות מיוחס להפרש scope מתועד. זה נשאר פתוח.

---

## שתי אפשרויות לפעולה הבאה — רז בוחר

### NA-A — לסגור את הפער demo → production *(מומלץ)*

- **למה:** הדמו מקדים ב־227 שורות, ובתוכן `d87d2c0` — הגנה מפני חריגת מכסת `localStorage` בנקודות שחזור, שבלעדיה **הייבוא נשבר לגמרי**. התיקון הזה יושב ב־demo בלבד בזמן ש־production חשוף לבאג.
- **מה נעשה:** להעביר את `2e4bc93`, `18f64dc`, `d87d2c0` + תיקון `F-14` (עדיין ללא commit) ל־`expense-app-v37.html`.
- **תנאי מקדים חובה:** אישור שלך שבדקת את שלושת התיקונים ב־demo מהאייפון — `https://razgeller4-rgb.github.io/raz-shira-expenses/expense-app-v37-demo.html`. ללא זה לא נוגעים ב-v37 (כלל ברזל ב־`CLAUDE.md`).
- **לפני העברה:** גיבוי נתונים. ⚠️ הפקודה ב־`CLAUDE.md` שבורה (ר' `R-02`) — הגיבוי יילקח מהקובץ האחרון בפועל, `backups/raz-expenses-backup-2026-09-09_15-32.json`.
- **סיכון:** בינוני — נוגע ב-production שמחזיק נתונים אמיתיים.

### NA-B — לתקן קודם את היגיינת בטיחות הנתונים

- **למה:** שני סיכוני `high` פתוחים ונוגעים בדיוק במה שהוגדר "המידע הוא המקור היחיד":
  - `R-01` — `.gitignore` מכיל `*.json`, כך ש־12 גיבויי הנתונים קיימים ב־iCloud בלבד, ללא היסטוריית גרסאות.
  - `R-02` — פקודת הגיבוי ב־`CLAUDE.md` מפנה ל־`raz-expenses-backup-LATEST.json` שאינו קיים. היא נכשלת בכל הרצה.
- **מה נעשה:** לתקן את הפקודה ב־`CLAUDE.md` (או ליצור symlink `LATEST.json`), ולהכריע על `D-04` — האם להוציא את `backups/*.json` מ־`.gitignore`.
- **סיכון:** נמוך — מסמכים והגדרות בלבד. אין נגיעה בקוד האפליקציה.

---

## אסור בשתי האפשרויות

- לגעת ב־`expense-app-v37.html` לפני אישור מפורש שלך על ה־demo
- `git push` ללא אישור
- למחוק או לשכתב קובץ ב־`backups/`
- לשנות קבצי `*.xlsx`

## בסיום

Checkpoint → עדכון `STATUS_CHANGELOG.md` (שורה בראש) + `PROJECT_STATUS_SNAPSHOT.json` → החזרת הקובץ הזה ל־`WAIT`.
