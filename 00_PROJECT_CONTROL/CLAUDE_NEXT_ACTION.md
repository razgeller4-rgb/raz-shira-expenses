# Next Action

עודכן: 2026-09-12

## Status

```
GO — שלב 6: עיצוב 10 המסכים. אין חוסם טכני.
```

---

## מה נסגר (12/09)

| | ראיה |
|---|---|
| **שלב 2ב** — מסלול הייבוא | F-17/18/19 נפרסו. 8 שורות של שירה: יולי 2 · אוגוסט 6 · 1,550.47 |
| **שלב 5** — מערכת עיצוב | 107 משתנים · 5 רכיבים · 30 וריאנטים · **0 ערכים מוקשחים** |
| `D-05` `D-08` `D-09` | כיוון ג׳ · חודש חיוב · pdf.js עצל |
| `C-1` | **הופרך** — רז בדק, המאזן והתחזית של שירה תקינים |

---

## הפעולה הבאה — NA-C · שלב 6

**לעצב את 10 המסכים מהרכיבים שנבנו**, לפי סדר תדירות שימוש: דשבורד → הוצאות → ייבוא אשראי → תקציב → משותף → חובות → הכנסות → יעדים → שנתי → כרטיסים+גיבוי.

**כלל מחייב מהתוכנית:** *"לא מעצבים 10 מסכים ואז מראים."* מסך אחד → אישור רז → המסך הבא.

**למה זה הצעד הנכון עכשיו:** המערכת בנויה ומאומתת, אבל **הרכיבים עדיין תרגום של כיוון ג׳ ולא קפיצה מעליו**. הקפיצה קורית בהרכבה למסך שלם — שם מתברר אם ההיררכיה עובדת.

---

## מה שפתוח ולא חוסם

| | סוג | הערה |
|---|---|---|
| **בדיקת שירה** ⚠️ | תזכורת | להעלות את ה-PDF בפרודקשן. **אומת תכנותית, לא בידיים שלה** |
| שלב 1 | היגיינה | `D-04` גיבויים ב-git · הרשאות סוכנים · 3 מפתחות ללא namespace |
| שלב 2 | אימות | 3 חשדות חישוב: C-2 ריבית · C-3 ספירה כפולה · C-4 dedup |
| `R-04` | סיכון | `cycleDay` = תחילת מחזור בקוד, יום חיוב בדף. **לא נגעתי** — מיגרציה של ~880 שורות |
| אייקונים · density | מערכת עיצוב | Tab item עם placeholder עגול |

---

## אסור

- לגעת ב-`expense-app-v37.html` בלי מעבר דרך `promote_demo_to_prod.py`
- `force push` / `reset --hard` / מחיקת branch ללא אישור מפורש
- לתקן חשד חישוב בלי מספרים אמיתיים + `domain-risk-reviewer` — **C-1 הוכיח למה**

## בסיום כל שלב

עדכון `STATUS_CHANGELOG.md` (שורה בראש) + `PROJECT_STATUS_SNAPSHOT.json` **באותו סבב**.
`L-1` חזר עליי היום: ה-snapshot פיגר ב-40 קומיטים עד שרז שאל.
