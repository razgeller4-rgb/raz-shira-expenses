.DS_Store
~$*
*.xlsx
*.json
!manifest-demo.json
!manifest.json
# ⛔ D-04 — אין להסיר את ההחרגה של backups/ מבלי לבדוק קודם את נראות הריפו.
# מרשם ההחלטות הציע (10.09) להוסיף `!backups/*.json` בנימוק "הריפו פרטי".
# הנימוק שגוי: `gh repo view` מחזיר visibility=PUBLIC — הריפו חייב להיות
# ציבורי כי GitHub Pages מגיש ממנו את האפליקציה. הוספת החריג הייתה דוחפת
# 5.7MB של נתונים פיננסיים אמיתיים (בתי עסק, סכומים, משכורות, יתרות הלוואה)
# למאגר ציבורי — פעולה בלתי הפיכה: git push ציבורי נשמר במטמון ובפורקים
# גם אחרי מחיקה. נבדק 14.09.2026.
.claude/worktrees/
.claude/settings.local.json
/data/private-grocery/
